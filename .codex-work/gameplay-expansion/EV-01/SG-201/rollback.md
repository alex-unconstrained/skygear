# Rollback

The immutable candidate is commit `b2cae3e`. The whole candidate was cut by
revert commit `2ce2313`; no gameplay subset was retained. The safe published
itch rollback remains build 65 (`65-deck-edge-verdict`), because SG-201 was
never published.
