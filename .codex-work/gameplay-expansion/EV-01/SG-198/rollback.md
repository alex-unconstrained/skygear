# EV-01 rollback

The complete candidate is retained at `94fbdc2`. It was cut whole at
`8ce043e`, which restores the pre-Muster gameplay path and releases the
collision queue. Do not cherry-pick only part of the candidate.

The itch rollback point is build 64, `64-muster-cut` (#1858373). Any repair
must be a separately claimed board item, retain SG-198's failed 7.96% verdict,
and rerun the unchanged 120-distinct-seed contract before unblocking consumers.
