# THE BOARD — the work queue

**This is where agents pick up work and where they report it.** One item, one
row, one owner at a time. `docs/OUTSTANDING.md` stays the ledger of what the
owner asked for — an owner ask lands there first and gets mirrored here as one
or more workable items. Bugs found by tools, assets to generate, and infra work
land here directly.

## The rules

1. **Claim before working.** Set STATUS to `IN PROGRESS` with your agent name
   and the date. Two agents on one item is wasted work.

   **AND IF YOU ARE IN A WORKTREE, YOUR NEXT-FREE ID IS A GUESS.** On
   2026-08-03 six agents ran in parallel off worktrees branched at different
   commits, and *four of them* filed rows as SG-155/156, SG-159/160/161 and
   SG-159/160/161/162 — every one of those IDs was taken by a sibling before
   the merge, and renumbering them afterwards meant rewriting IDs across code
   comments, check names, design docs and this board. The next ID is not a
   fact you can read off a stale branch. Either take a block from the person
   dispatching you, or expect to be renumbered and keep your ID in as few
   places as you can manage.
2. **DONE needs evidence, and so does every coverage claim in the docs.** A
   named harness check string, a tool output, or a commit hash — never "looks
   right". This adopts the audit rule, and it holds REPO-WIDE, DESIGN DOCS
   INCLUDED: any sentence in any `.md` (STATUS, README, the design docs, this
   board) that says the harness covers/pins/guards/asserts a behaviour must name
   the check string it means, or be marked `UNVERIFIED —` until it can. A claim
   of coverage that names no check is evidence of nothing (SG-11).
3. **Found a bug? File it before fixing it.** One row, source named, even if
   you fix it in the same session. The ledger exists because things slipped
   when nobody wrote them down.

   **3a. "THE BROWSER DID IT THIS WAY" IS NOT A REASON FOR ANYTHING.** Not for a
   mechanic, not for a visual, not for a constant. The owner retired parity as a
   goal on 2026-08-01 and hardened it to mechanics on 2026-08-03, after SG-148
   took crit away from six damage sources on fidelity grounds alone and had to be
   reverted: *"We've moved so far beyond the browser version of this game. I do
   not want you going back and trying to refer to the browser game. We've
   transcended that. We should only be focused on making the Godot version of
   this as good as possible."*

   Recording where a number CAME from is good practice and stays. Using that
   provenance as the argument for keeping it is the thing that is banned. If a
   change's whole justification is browser fidelity, stop and ask the owner —
   and if a number is right, say why it is right FOR THIS GAME. The frozen
   browser build in `reference/` is an archive and a regression yardstick
   (`tools/parity.py` — "did we lose something that was good"), never an
   authority, and the answer may legitimately be "yes, and we keep ours anyway".
4. **BLOCKED names the blocker** — a missing key, a decision only the owner can
   make, an upstream item by ID. Anything blocked on the owner is also
   mirrored, with a recommendation, in `/NEEDS_ALEX.md` at the repo root.
5. **Nothing is deleted, and THIS FILE HOLDS ONLY OPEN WORK.** The moment a row
   is DONE, CUT, DROPPED, CLOSED, RESOLVED, SUPERSEDED, CORRECTED or ANSWERED,
   cut it — with its evidence or reason intact — into
   `docs/BOARD-ARCHIVE.md`. `git log` on both files is the audit trail.

   **This is the owner's instruction, 2026-08-11**, and the reason is the size
   the file had reached: 758 KB, 233 rows, **190 of them history**. A queue
   nobody can read is not a queue. Do not re-grow it — a row that has just
   passed its gate does not "sit in Active for a while first".

   **A row that is only PARTLY resolved stays here.** "HALF DONE — STILL OPEN",
   "FIXED — PENDING VISUAL CONFIRMATION" and "three of four surfaces done" are
   open rows; the archive is for work with nothing left in it.
6. **Priorities:** P1 = blocks shipping or contradicts a written claim ·
   P2 = owner-visible improvement · P3 = worth doing, not worth doing first.
7. **Before starting anything**: read `STATUS.md`, run
   `SkyGear Tools.bat harness` (green before AND after your change), and check
   this board's IN PROGRESS rows so you don't collide.

**Where write-ups go.** `/NEEDS_ALEX.md` is a **page**, not a dossier — it holds
only what needs the owner's judgement, and it has had to be cut back from 600+
lines twice in one day. **Append your evidence to `docs/NIGHT-LOG-<date>.md`,
not to NEEDS_ALEX.** If something genuinely needs his decision, add a short
entry to its decision list and put the numbers in the log. A section he will not
read because the file is six hundred lines long is the same as not writing it.

## Active

| ID | P | Type | Title | Status | Notes |
|----|---|------|-------|--------|-------|
| SG-241 | P2 | UI | **The UI/UX demo-readiness slice plan — S1 shipped, S2 and S3 are the next two and both want `hud.gd`** | IN PROGRESS — filed 2026-08-11 by opus/coordinator from the owner's ask (*"a proper UI/UX pass … a lot of issues with text, a TON of overwhelming information to a new player"*) | Full evidence in **`docs/UI-UX-AUDIT-2026-08-11.md`** — a static source audit, every claim a quoted string with a `file:line` or a count off the draw code, and every visual claim explicitly marked PENDING VISUAL CONFIRMATION because the GPU was parked when it was written. **S1 · DONE**: `_draw_results` folds the tester report behind a `DETAILS` control — default view went **22 distinct drawn strings → 12**, DR-12's monospace-padded table is redrawn as cells on fixed x-rails inside the fold, and the clipboard is provably byte-identical (`results · the clipboard report is byte-identical before and after`, proved non-vacuous by corrupting the copy payload). Harness **1208/1208**. `RESULTS_DETAILS_OPEN_BY_DEFAULT` inverts the fold in one line if the owner wants the full report as the default face. **S3 · NEXT**, `hud.gd` only: purge development vocabulary from shipping surfaces — `"TABLED — an interaction pass will revisit"` (`hud.gd:5209` and `:5231`), `"tabled with the crate-verb family"` (`:5237`), the Workshop's self-deprecating `"all a tree this small has to offer"` (`:5038`) — and delete `_pause_text()` (`hud.gd:5424`), which has **zero callers** and carries two stale claims (`"Space dash"`, false for the Boilerwright; `"1/2/3 pick a card"`, when SG-46 deals four). **S2 · CLEARED, NOT STARTED**: SG-93's menu vocabulary spread to SETTINGS / HOW TO PLAY / CONTROLS / PAUSE — the owner's *"proceed as you see fit"* on 2026-08-11 is the verdict SG-93 was blocked on. **SERIALIZE S3 THEN S2**; both own `hud.gd` and cannot run together. **Also live and NOT swept into these slices**: SG-161 (settings footer printed through BACK), DR-09 (PAUSE labelled `"1"` like MOVE UP and **cannot be rebound at all** — 11 rows numbered `(i+1) % 10`), DR-17 (everything is the engine fallback font). **100 fresh captures exist** at `.shots/screens/polish-audit/` (25 screens × 4 widths, 2026-08-11) — the upscaled comparison pass over them is the next coordinator job, and `.shots/screens/*.png` at the TOP level are STALE (pre-build-70; they still draw `"SKYGEAR — Godot port"`) — do not audit those |
| SG-240 | P2 | ASSET | **33 already-paid Loom generations were never ingested, and every target file they would replace already exists** | OPEN — needs the owner's candidate picks; filed 2026-08-11 by opus/coordinator | **The SG-105 blocker was a stale path, not a missing tool.** `tools/forge.py` had `LOOM_DIR` pointing at `C:/Users/alexr/Documents/Codex/2026-07-26/done-66-images-fully-specced-for`, which does not exist; the Loom now lives at `C:/Users/alexr/OneDrive/Documents/GitHub/AetherLoom` and the constant is fixed. **59 forge-job folders survived the move**, four candidates each, generated 2026-08-11 14:22. `forge.py list` reports `hud` at **9 assets, 0 delivered, 21 generations** and `weapon` at **3 assets, 0 delivered, 12 generations` — so nothing needs regenerating and **nothing was spent**. Contact sheets built at `<scratch>/loom/*.png`, one per asset, candidates side by side on a checker. **TWO REASONS INGEST WAS HELD.** (1) For the seven four-candidate assets `pick` defaults to candidate 1, so ingesting would silently make an aesthetic choice that belongs to the owner. Coordinator's recommendations, for what they are worth: `ui_plate_wide` **2** (thinnest border, most interior for text — 3 is the prettiest object and the worst container), `ui_plate_slot` **4** if the top tab is wanted as a home for the key binding else **2**, `ui_bar_housing` **4** (closest to SG-105's 8:1 trough — **rule 3 out regardless of taste, its leather straps cross the interior and a fill bar sliding under them reads as broken**), `ui_pressure_dial` **1**, `weapon_cutlass` **2**, `weapon_gearblade` **2**, `weapon_boarding_axe` **3**. (2) **Every target file already exists** in `assets/art/ui/` — `plate_wide.png`, `bar_housing.png`, `pressure_dial.png`, `cooldown_sweep.png`, `dash_pip.png`, `lane_track.png`, both bar fills — so ingest overwrites live art, and `forge.py list` calling them "0 delivered" contradicts what is plainly on disk. **Resolve that contradiction before overwriting anything.** `existing()` guards `cmd_ingest` unless `--force`, but the coordinator bypassed `cmd_ingest` in planning because `poll()` needs the Loom server and the Loom's `.venv` is broken (it points at a uv-managed Python that did not survive the move); `src/loom-ingest.py still <file> <key>` takes a path directly and needs no server |
| SG-239 | P1 | INFRA | **The audio pipeline has never delivered into the Godot game — `ingest-audio.py` serves only the browser build** | OPEN — filed 2026-08-11 by opus/coordinator per rule 3, **found by doing it**: the SG-228 captain recast generated 50 correct takes that could not reach the game | `src/ingest-audio.py` writes `src/storm-dusk/_audio_index.js` and **contains no reference to `skygear-godot` anywhere**. The documented chain in `tools/soundforge.py`'s own header — *"Then, always: `python src/ingest-audio.py --from audio` && `python tools/audio-check.py`"* — therefore leaves the shipping game untouched. Every `.ogg` under `skygear-godot/assets/audio/` was dated **2026-08-01** and came from some other environment. **The gap is an ENCODER, and this machine had none**: `soundforge.py` emits `.wav` masters, the game wants `.ogg`; PyAV's build has no `libvorbis`, its native `vorbis` encoder is flagged experimental and refuses `avcodec_open2` even with `strict=experimental`, torchaudio needs TorchCodec which is absent, and `choco install ffmpeg` fails without an elevated shell (`Access to 'C:\ProgramData\chocolatey\lib-bad' is denied`, **0 packages installed — nothing on the machine changed**). **The workaround that unblocked SG-228 is NOT a fix and must not be mistaken for one:** a static ffmpeg 7.1 with `libvorbis` was fetched by `pip install --target <scratch> imageio-ffmpeg` and used as `ffmpeg -i in.wav -c:a libvorbis -q:a 4 -ar 24000 -ac 1 out.ogg`. **It lives in a session scratch directory that will be deleted** — it is not on PATH, not in the ComfyUI venv, and not in the repo. **The real fix** is a Godot-side ingest that owns the encode, plus a documented, durable ffmpeg. Until then every future audio regeneration hits this same wall, and `soundforge.py`'s "then, always" instruction is wrong for the project that actually ships |
| SG-228 | P1 | DECISION | **The Captain is male in the art and female in the writing, the casting and the voice — the owner says male is correct** | **THREE OF FOUR SURFACES DONE 2026-08-11 — ONLY THE ART REMAINS OPEN.** ✅ **CASTING**: `docs/VOICE-BRIEF.md` §2 recast to a young male, with a new §3a recording that the art was right and the writing was wrong so any remaining feminine phrasing reads as a leftover rather than a second opinion; `tools/soundforge.py` repinned. ✅ **VO**: **two complete candidate sets were generated and judged by ear** — the owner chose **Will** (`bIHbv24MWmeRgasZH58o`, young, relaxed, American) over Charlie (`IKne3meq5aSn9XLyUdCD`, young, deep, Australian). 50 takes, **0 failed**, `audio-check.py` reports **0 of 50 masters need regenerating or correction** at −8.0 dBFS, encoded to 24 kHz mono `.ogg`, Godot reimport exit 0, harness **1208/1208**. Verified by duration rather than assumed: `victory_2` installed at **2.36 s** (Will) not 2.08 (Charlie), `wave_start_4` 0.79 not 0.71, `defeat_3` 1.01 not 1.18. **Charlie's 50 takes are kept, not deleted**, in `/casting-ab/` with a README, so the decision can be re-heard instead of re-argued — and that folder sits deliberately **outside `audio/`** because `ingest-audio.py` globs `audio/**` recursively and would sweep a comparison set into the delivery index as a second take of every line. Only 14 keys were regenerated: the boss and crew takes are untouched, and `vo_cannon_down` looks like a captain cue but is crew (*"She's down, Captain!"*). ✅ **TEXT**: all five player-facing strings flipped — `game_data.gd:247/248/305/307` and `hud.gd:633`; `grep` for gendered captain text in `scripts/` now returns nothing. **A TRAP WORTH KNOWING: the "she"/"her" still in the line sheet are THE SHIP** — *"Get them off her"*, *"She won't hold at this"*, *"That's it. She's going down"* — correct nautical register, and a warning now sits above §4 of VOICE-BRIEF so nobody runs a blind find-and-replace over it. ❌ **ART, STILL OPEN**: `assets/art/ui/portrait_corsair.png` remains a blue-coated, red-haired woman matching neither the sprite nor the model in gender, costume or palette — and it is the **only** portrait in the project, which is why the Boilerwright wears it too (SG-105). It needs redrawing, not recolouring; the Loom is unblocked for it (SG-240). **The infrastructure gap this exposed is SG-239.** Original filing follows — **owner-reported**: *"there's a disconnect between the captains gender in-game, in art and in audio. The captain is supposed to be male throughout. We need new VO for a young male whose voice matches the captain"* | **This is not drift, it is a specification that disagrees with the art.** `docs/VOICE-BRIEF.md:46` casts the CAPTAIN as *"A sky-pirate captain, 30s, weathered, dry … the worse it gets, the flatter **she** sounds"*, and `tools/soundforge.py:57-60` pins that to **Lily — middle-aged, British** (`pFZP5JQG7iQjIQuC4Bku`) with a female alternate named in the same comment (Arabella, raspy). Meanwhile `assets/art/heroes/corsair_front_*.png` and the `captain` 3D model both depict a **young male** — red brass-buckled greatcoat, goggles in spiked brown hair, brass gauntlet, curved cutlass — and that is the figure the player actually sees on the deck for the whole run. **THE OWNER'S RULING: MALE, AND YOUNG.** Four surfaces have to move together, and the ART IS THE ONE THAT IS RIGHT — nothing in `assets/art/heroes/` or `assets/models/captain` should change: **(1) CASTING** — `VOICE-BRIEF.md` §2 row and §3, plus the `VOICES`/comment block at `tools/soundforge.py:57-67`, need a young male voice pinned **by id** (the file's own rule: *"a voice picked by NAME in a later session is a different voice"*); the brief's other direction — North Atlantic working-class, mid-low, working-not-performing, no quips — is orthogonal to gender and should survive the recast, but *"30s"* becomes young. **(2) VO** — **50 captain takes** in `assets/audio/voice/captain/` are the old casting and all need regenerating through `python tools/soundforge.py voice`; `ELEVENLABS_API_KEY` is present at User scope (51 chars) so this is **unblocked but COSTS MONEY and needs the owner's explicit go**. **(3) TEXT** — five player-facing strings say her/she: `game_data.gd:247` (*"Fills **her** gauge"*), `:248` (*"**Her** gauge fills from damage landed…"*), `:305` (*"means nothing to **her**. **Her** gauge comes from the fight"*), `:307` (*"**her** spot moves, and she chases it"*), and `hud.gd:633` (*"**Her** basic attack, and the only weapon every run has"*) — plus design-doc comments (`game_data.gd` ≈230-243 contrasts *"**Hers** fills by itself"* against *"**His** fills ONLY where he plants himself"*, which is the Boilerwright and stays). **(4) ART** — `assets/art/ui/portrait_corsair.png` is a **blue-coated, red-haired woman** and matches neither the sprite nor the model in gender, costume or palette; it is also the ONLY portrait in the project and is what the Boilerwright wears too (SG-105). It needs replacing, not recolouring. **Serialize the text half after S1/S2/S3** — `hud.gd` is already held by the UI slices |
| SG-227 | P3 | ASSET | **Repaint the Captain's swing plate so the hand-painted crescent can come back** | OPEN — filed 2026-08-11 by opus/coordinator, **the second half of the owner's SG-226 verdict**: *"generated now, repaint later"* | SG-226 retires `assets/art/fx/slash_arc.png` from the `"arc"` decal path because its ink sits on the wrong side of its own aim axis — measured twice independently: **83.1% of alpha in the −X half, 0.0% within ±30° of +X, centroid 162.4° off aim**, peak bin 150–180°. The generated rim-fan replaces it and closes the P1, but that is an **evidenced exception to this project's `view3d.gd:3732` doctrine that painted beats generated**, not a preference. **The asset is deliberately NOT deleted** — it is the starting point for the repaint. **Spec for the new plate:** crescent centred on **+X**, the decal's aim axis, since `_decal` builds basis X as `(cos a, 0, sin a)` and `_fan_texture` opens along +X by documented convention; band no wider than the **140° cone** it represents (the current plate spans ~±150°, so even rotated 180° it would overdraw the damaging arc — rotation is NOT a fix); solid crescent rather than a rim, because the generated replacement is a rim (`filled = false`) and the swing may now read thinner than it did — **that specific risk is flagged UNCONFIRMED pending windowed capture**. **Do not close SG-226 into this row**: SG-226 is the correctness fix and stands on its own; this is the art restoration, and the game is correct without it. Per [[alex-asset-workflow]] the spec is ratios and a descriptive prompt, never pixel dimensions. Candidate producers now that both exist locally: the Aether Loom (needs the OpenAI key, now set at User scope) or ComfyUI, which currently has **no still-image model installed** — see the coordinator's note on `C:/ComfyUI-models/` |
| SG-226 | P1 | BUG | **The Captain's cleave swing effect draws away from the bodies it is damaging — found by Alex in live play** | **FIXED 2026-08-11, AUTOMATED GATES GREEN — PENDING VISUAL CONFIRMATION (windowed capture deferred by the owner's GPU park).** Owner's verdict was *"generated now, repaint later"*; the repaint is filed as **SG-227**. One code line in `view3d.gd` ≈5209: `_art("slash", _fan_texture(…))` → `_fan_texture(float(fx.get("arc", 1.7)), false)`, with a comment block above it recording the measured evidence, citing SG-63's precedent and naming this an **evidenced exception** to line 3732's *"painted beats generated"* — not a preference. `slash_arc.png` is deliberately NOT deleted; no other `_art()` caller touched. New check, added beside SG-63's own: `vfx · the cleave's slash mark keeps its ink ahead of the aim axis, where the damage lands — the SG-226 regression`. It appends a real `"arc"` effect, runs the real `_sync_effects()`, and reads the **RESOLVED** texture off the production path (`view._decals[…].texture_albedo`) rather than re-deriving it, so rewiring the branch back to the plate fails it. **Non-vacuous in both directions, verbatim:** with the old wrap restored → `FAIL … 0.0% of resolved-texture alpha within ±30° of +X against a floor of 25%`, `1203/1204`; with the fix → `ok … 62.0% … against a floor of 25%`. Harness **1204/1204, exit 0, 0 raised, 56 against the pinned 56**, `git diff --check` clean — **re-run and confirmed independently by the coordinator**, not quoted from the builder. **THE ONE OPEN RISK, UNCONFIRMED:** the generated fan is a RIM (`filled = false`, `smoothstep(1.0 - FAN_RIM_W * 2.0, 1.0, d)` — an outer band with nothing under it) where the retired plate was a solid crescent, so the deck mark very likely reads THINNER than before. Mitigating and equally unconfirmed: the mark now appears on the side the player is actually looking at, and the blade trail carries most of the swing's weight in the air. If it reads weak under windowed capture the candidates are the filled variant (`true`, as the cone uses) or SG-227's repaint. **DIAGNOSIS THAT LED HERE — reading (A) SPATIAL, mechanism is the PAINTED PLATE, and it is PRE-EXISTING, not AB-04's**. The angle math is clean: a headless probe (body due north, aim −90°) shows the fx stamps the ROTATED damaging direction (stamp − sim = 0.00° on both beats), `side` matches the latched beat, and `cleave_lead` is a correctly-signed transient (±31.5° at spawn → 0° at landing). The defect is `view3d.gd` ≈5192 `_art("slash", …)`: the arc decal's +X axis points at the damaging direction, but `assets/art/fx/slash_arc.png` (384×256, in the repo since v12 commit 151ec7f) carries **82.8% of its alpha in the −X half of the quad, 0.0% within ±30° of +X, ink centroid 163° off aim** — **INDEPENDENTLY RE-MEASURED BY THE COORDINATOR** from the 384×256 PNG with a separate script: `front (+X half) 16.9%`, `back (−X half) 83.1%`, `within ±30° of +X: 0.0%`, `centroid 162.4° off aim`, and the 30° histogram shows the two bins straddling the aim axis (`0-30` and `330-360`) both at **exactly 0.0%** with the peak at `150-180` (17.3%); provenance confirmed too — `git show c27ca88:…view3d.gd` carries the identical `_art("slash", …)` draw at line 5088 and `slash_arc.png` last changed in `151ec7f` *"v12: the run was too easy…"* — — so the crescent paints on the deck on the OPPOSITE side of the captain from the cone that resolves. `git show c27ca88:…view3d.gd` L5085-89 has the identical `_art("slash", _fan_texture(…))` draw, so AB-04 (704a304) added only the ≤31.5° lead; the 180° throw is longstanding and newly noticed beside the new (correct) tell. Not reading (B): the decal cull_mask excludes LAYER_FIGURES (≈5769), so nothing renders under her model — the ink genuinely sits behind her. Smallest correction: drop the `_art("slash", …)` wrap on the `"arc"` branch and draw `_fan_texture(arc, false)` directly (the same generator family the approved tell uses), plus an SG-63-pattern harness check that any arc-decal texture keeps a floor share of its alpha inside the damaging arc about +X. NOT fixed yet — diagnosis only; filed 2026-08-11 by opus/coordinator per rule 3, **owner-reported from hands-on G5 play** | Alex, verbatim: *"The cleave indicator is correct, but the vfx is triggering behind the player?"* **The tell is not the problem — he is happy with it, which is the first half of SG-208's G5 answered in the affirmative.** The swing arc is. Reproduced in an existing capture: in `.shots/clips/cleave-after/frame_0030.png`, zoomed, the captain is engaging two automatons standing **north** of her — the `48/40/20` and `24/40/20` floaters prove both are taking cleave damage — while the red swing arc sweeps **across her legs and to the south**, away from the bodies being hit. The pale tell wedge sits correctly lateral in the same frame. **Two readings are being distinguished before any fix**: (A) SPATIAL — the drawn angle disagrees with the simulation's damaging angle; (B) DRAW ORDER — the arc is roughly right but renders under the captain's model so only the far side shows. Prime suspects, in order: `view3d.gd:5121` `cleave_lead()` with `CLEAVE_SWEEP := 0.55` — **0.55 rad is 31.5°, a large lead added by AB-04** which compounds with the simulation's own ±12° for ~43° of off-axis throw at spawn; and `view3d.gd:5168` `centre := fx.position`, a spawn-stamped position that cannot follow a captain who moves during the 0.36 s swing. **WHETHER THIS IS NEW IN AB-04 OR LONGSTANDING AND NEWLY NOTICED IS THE FIRST THING BEING SETTLED** — baseline for the comparison is `704a304^` = `c27ca88`. **NEITHER CRITIC CAUGHT THIS**: both systems and presentation passes were aimed at whether the *tell* read, and the swing effect's own aim was never anybody's question. That is a gap in the critic contracts, not just in the code |
| SG-225 | P2 | UI | **The Cleave tell's camouflage floor is measured against bare planking, but its real competitors are the lit pools the check never scans** | OPEN — filed 2026-08-11 by opus/coordinator per rule 3, found by the SG-208 round-2 presentation critic, **deliberately not fixed in that round** (it is a separate slice and AB-04 had already reached PASS) | The SG-208 fix made the next-beat tell readable — on quiet deck at a glance, and in a crowd **found rather than seen**. Both the round-2 critic's blind pass (pair 2, crowded, wave 3, 14 boarders) and its own fresh `crit2_fight/frame_0025.png` and `frame_0055.png` cost it a one-to-two second hunt among the brazier and lantern pools. **The check cannot see this and stays green**: `tests/parity_test.gd:1730-1740` derives `deck_peak` by scanning only `_planking_texture`'s authored hex tones, so it pins the ink against **bare plank albedo 0.40** while the deck's real brightness under a brazier is higher, and the frost/fire rings and mortar VFX it competes with are never scanned at all. **Why it matters:** the tell is the only pre-swing information pricing ground — inside 110 through the return pays 24, the edge pays 20 — and that decision is made mid-crowd, precisely the frame where the tell is weakest. A player who can only read it on quiet deck learns the price of ground exactly when ground is free. **Smallest correction:** extend the same `deck_peak` scan to the brazier/lamp-pool painter's authored tones so the ink must clear the brightest **ambient pool** by 0.30; if that raised floor fails, the one production dial is `view3d.gd:5070` `CLEAVE_TELL_LIFT` (0.5 → whatever clears it). No simulation file is touched. **Proof required:** the check printing the higher peak while the run stays green, a negative control turning that check red, and one fresh `fight` frame where a fresh-context viewer writes *"found without hunting"* having never seen the diff. **Also record here, ruled non-blocking by the round-2 critic:** the Boiler's dome visibly clips the wedge's lower edge when the captain fights just north of it (`crit2_fight/frame_0025.png`) — the notch rides ±0.95 rad off aim and lands lateral in practice, so the side information survived in every captured frame; degrades, does not defeat |
| SG-208 | P1 | GAMEPLAY / AB-04 | **Captain Cleave alternates a readable port cut and stronger close-range starboard return** | **IN PROGRESS — IMPLEMENTED AND SHIPPED, CRITIC VERDICT DOES NOT EXIST** — opus/coordinator 2026-08-11. **THIS ROW WAS STALE AND IS NOW RECONCILED.** It read `SAFE STOP 2026-08-05 after red-first; no production delta retained` and told the next agent to begin implementation again from the dispatch — that has been false since 2026-08-11. AB-04 **was** implemented, at commit `704a304` (parent `c27ca88`), and it **is in shipped itch build 70** (`70-demo-polish-pass`, #1875503) per SG-224. Delta is 732 insertions across `scripts/game.gd` (+130), `scripts/game_data.gd` (+26), `scripts/view3d.gd` (+88), `tests/parity_test.gd` (+496). Harness at HEAD re-run by the coordinator and confirmed **1202/1202, exit 0, 0 script errors, 56 engine errors against the pinned 56** — measured, not quoted. **WHAT IS MISSING IS THE VERDICT:** the critic gauntlet that was to judge this packet is the process that froze the owner's machine, so it was killed mid-run and never returned. **TREAT CLEAVE AS UNTESTED, NOT AS APPROVED.** Fresh-context systems and presentation critics are being run serially (not concurrently) by opus/coordinator on 2026-08-11 to avoid repeating that freeze. **SYSTEMS CRITIC: PASS (2026-08-11, fresh context, reproduced 1202/1202 itself and read all four diffs rather than the commit message).** Every §17.7 bullet HOLDS behaviourally, named checks: `cleave · odd cuts centre twelve degrees to port and even cuts twelve degrees to starboard` → `odd port 20 star 0, even port 0 star 20`; `cleave · two connected close cuts total 44 and two cuts at the edge total 40` → `close 20 + 24 = 44, edge 20 + 20 = 40`; `cleave · a swing that finds no body and no hull advances neither serial, beat nor cooldown` → `miss quiet true, corpse quiet true`; `cleave · the deck shows the side that enters next before a target is acquired, on no presentation timer` → `empty deck tell at -2.521, after one cut -0.621 (aim -1.571)`; `cleave · the hull takes the resolved 20 or 24 once through the ordinary splash`. **THE PROJECT'S AUTHORED-BUT-DEAD-FIELD FAILURE MODE DOES NOT RECUR HERE** — all four new fields have production readers and `cleave_close_damage` computes `base * close_scale`, so 24 is derived from the authored 1.20 rather than hardcoded (contrast SG-164 `dps`, SG-121 `radius`, SG-171 `grip_at`). **ONE GAP FOUND AND CORRECTED IN THE SAME SESSION:** `auto.damage: 22.0` is no longer read by the swing but is still the number the kit row and comparison screen advertise, and nothing coupled it to the 20/24 actually paid — a later tuning pass could have moved `combo_damage` and left the price tag behind, green. `tests/parity_test.gd` check 1 now carries `and is_equal_approx(float(auto.damage) * 2.0, shipped_total)`; **proved non-vacuous** by drifting `game_data.gd` to `23.0`, observing `FAIL ... advertised 23 x 2 = 46` at `1201/1202`, and restoring (`git diff` on `game_data.gd` empty afterwards). Harness after the fix: **1202/1202, exit 0, 0 script errors, 56 against the pinned 56**; `git diff --check` clean. **PRESENTATION CRITIC: FAIL (2026-08-11, fresh context, motion evidence via `tools/clip.gd` — 260 rendered frames across three scenarios, viewed).** **THE §17.7 KILL CONDITION APPLIES AS SHIPPED.** The next-beat tell is drawn correctly — target-independent, timer-free, correctly side-offset, present on an empty deck — but it is a **~20–30 pixel, ~0.30-alpha ember-orange notch on ember-lit orange-brown planking**. The settling observation: the critic, hunting for it with the diff open, **catalogued it as a deck prop** and needed 3× zoom plus 1.5× brightening to confirm what it was; in a fight frame it vanishes among brazier pools, lantern wash and an unrelated orange ring. The in-game yardstick that already passes at the same distance is the furnace knight's red telegraph wedge. **This is a contrast/hierarchy defect in one decal call, NOT a defect in the mechanism — so the disposition is fix-and-re-review, not CUT.** **A SECOND FINDING IS ARGUABLY WORSE:** no clip scenario in the repo puts a body inside the Captain's 190-unit reach for consecutive basics — in `fight` the crew and cannons intercept every boarder first, in `knight` they die mid-deck — so §17.7's second clause (*"the two attack animations/effects visibly enter from those opposite sides"*) **has never been witnessed by anyone**, and the tell has never been seen to flip. Round 1 of ≤4 dispatched 2026-08-11 (opus/coordinator): raise contrast per SG-162's own *"a fill you can see through and an edge you cannot miss"*, add a scenario that exercises repeated connecting basics, pin the contrast with a non-vacuous check. Allowed files `view3d.gd`, `parity_test.gd`, `tools/clip.gd`; simulation untouched. **ROUND 1 BUILT, ROUND 2 CRITIC: PASS — THE CUT CONDITION NO LONGER APPLIES (2026-08-11).** Fix: `view3d.gd` `_sync_cleave_tell()` now draws the **filled** fan texture (SG-162's fill/edge split lives in that texture, so the ratio was adopted rather than restated) at `CLEAVE_TELL_ALPHA 0.80` with ink `tint.lerp(Color.WHITE, CLEAVE_TELL_LIFT 0.5)`, replacing raw element hue at 0.30. **Size unchanged** — at full contrast it reads without growing, and it stays quieter than the furnace knight's red wedge. **THE MISSING EVIDENCE TOOL WAS THE REAL UNLOCK:** a new `cleave` scenario in `tools/clip.gd` stages boarders inside the 190 with crew unmustered, lane guns held reloading and knock zeroed as `_cleave_pair` zeroes it, so consecutive basics finally resolve on camera. **§17.7's second clause is now WITNESSED for the first time in the packet's life** — `crit2_cleave/frame_0000.png` puts the notch to starboard, `frame_0052.png` to port one resolved cut later, both readable at a glance unzoomed. Round-2 critic was run **blind** on eight neutrally-labelled frames with a deliberately mixed A/B mapping and **identified the corrected frame correctly in both discriminating pairs before reading any source** (it honestly reported it could not separate tell from swing in the two mid-swing pairs, which is correct — the orange crescent dominates there). Over-correction checked for and absent: the tell sits above scenery, below threats. New check 16 `cleave · the tell's ink outshines the deck's brightest plank in every element — the SG-208 camouflage floor` → `decal mix 0.80, ink luminance 0.78 over deck peak 0.40, dull elements []`; **non-vacuous**, proved by reverting the alpha to 0.30 (`FAIL … 1202/1203`) and restoring. Harness **1203/1203, exit 0, 0 raised, 56 against the pinned 56**, verified independently by the coordinator, `git diff --check` clean. **RESIDUAL GAP FILED AS SG-225, NOT SWEPT UP** — in a crowded frame the tell is *found* rather than *seen*. **G5 IS STILL ALEX'S AND HAS NOT BEEN SIMULATED** — the question to put to him in a live run at wave 3 or later is *"without looking at numbers, which side is the next cut coming from, did you notice when you stood close for the big one, and does anything hide the marker when you fight beside the Boiler?"* G5 is Alex's and has not been asked for. **RELEASED REMAINS UNAVAILABLE** — combat publication is held behind the combined IN-00 audit (AB-01–04, EL-00–03, RF-01 all stable), and no AB-02/AB-03 verdict changes that | Resume at commit 4dc96cb. The frozen red-first harness is 1187/1200 with exactly thirteen new cleave contracts red, every prior check green, zero script errors and the pinned 56 engine errors; artifacts are in .codex-work/gameplay-expansion/AB-04/SG-208/. All interrupted production and test edits were reverted to HEAD, so implementation must begin again from the documented dispatch rather than continue a partial hunk. BASE-00 is accepted and SG-207 is measured ahead of this development-only cursor. Allowed files remain game_data.gd, the narrow Captain-basic seam in game.gd, view3d.gd, rig3d.gd only if the existing animation reader requires it, telemetry.gd only if ordinary attribution genuinely requires wiring, parity_test.gd, and ignored evidence. Keep 0.36/190/2.443/150; author 20/20, ±0.20944, 110 and 1.20. G3 remains CLEAVE1–CLEAVE120 with no tuning. **THE QUEUE-ORDER GATE IS CLEARED AS OF 2026-08-11**: Alex passed SG-206 and SG-207 in one verdict (*"I approve of field and pulse changes. Those are unblocked now."*), so SG-208 may now reach HUMAN-VERIFIED and MERGED on its own gates and may unblock EL-03/AB-05. **RELEASED IS STILL NOT AVAILABLE TO IT** — combat publication is held behind IN-00, which requires AB-01–04, EL-00–03 and RF-01 stable, and AB-04 is the packet in hand. Do not record this packet as released on the strength of the AB-02/AB-03 verdict. |
| SG-209 | P1 | MEASURE | **The demo-readiness ledger — 29 slices between "feature-complete and playable" and "Steam-demo-ready"** | OPEN — filed 2026-08-11 by opus/coordinator from a 15-agent read-only audit; **the ID block SG-209…SG-237 is issued here** | Full evidence in `docs/DEMO-READINESS-AUDIT-2026-08-11.md`. Seven auditors took one quality dimension each; every finding was then handed to an adversarial verifier told to refute by default and to open each cited `file:line` itself. **45 survived, 6 were refuted, and nine specific claims were struck outright** — §6 of the doc records what was struck and why, which is as much of the result as what survived. **TWO LIMITS THAT BIND ANYONE WORKING FROM IT.** (1) **No Godot was run** — a builder held the write side and concurrent `godot --path .` thrashes the shared `.godot` import cache, so everything is read off source, authored data and existing logs. **§5 lists every claim that cannot be asserted until measured against a running build**, including whether the telegraph reserve is ever reached, the magnitude of the first-rig-load hitch, and whether this build runs acceptably on any machine that is not the RTX 5080 of the project's single recorded frame profile (`docs/BOARD.md:241`). Nothing in §5 may enter a packet's justification until it has been measured. (2) **Nothing here has been seen or heard** — every "feel"/"reads as"/"cheap" is inference from constants. **DISPATCH BY SYMBOL NAME, NEVER BY `game.gd` LINE NUMBER**: cites were taken against two snapshots while SG-208 was in flight (§0-B verifies `damage_enemy` at `:3430` HEAD vs `:3541` live). Mapping: DR-01a→SG-210, DR-01b→SG-211, DR-02→SG-212, DR-03→SG-213, DR-04→SG-214, DR-05→SG-215, DR-06→SG-216, DR-07→SG-217, DR-08→SG-218, DR-11→SG-219, DR-14→SG-220, DR-19→SG-221, DR-22→SG-222; DR-09/10/12/13/15/16/17/18/23/24/25/26/27/28/29 remain in the doc and take SG-223…SG-237 as they are promoted. Five items are already owned by existing rows and were NOT re-filed: **SG-183**, **SG-105**, **SG-161**, **SG-192**, **SG-93**. |
| SG-223 | P2 | BUG | **Renaming `config/name` orphans every save the player has — found by doing it** | OPEN — filed 2026-08-11 by opus/coordinator per rule 3, **caused and reverted in the same session** | SG-210 changed `project.godot`'s `config/name` from `"SkyGear: Godot Port"` to `"SkyGear"` to fix the alt-tab label, because nothing in the project calls `window_set_title`. **That string is also what Godot derives `user://` from** — `%APPDATA%/Godot/app_userdata/<sanitised name>` — so the running game immediately began looking at a new, empty `app_userdata/SkyGear/` while `runs.json` (80,218 bytes, 60 rows), `workshop.json`, `keys.cfg` and `settings.cfg` all sat untouched under `app_userdata/SkyGear- Godot Port/`. **Nothing was lost** — the new directory was verified to contain 88 files, all of them shader cache and logs, and zero `.json`/`.cfg` — and the rename was reverted with the coupling written into the file as a comment. **THERE IS NO CONFIG-ONLY FIX.** `use_custom_user_dir` + `custom_user_dir_name` relocates the path too (to `%APPDATA%/<name>`), so it orphans the same saves by a different route. The rename needs a **migration in the same commit**: on startup, if the current `user://` has no save files and the legacy directory does, copy `runs.json`, `workshop.json`, `keys.cfg` and `settings.cfg` across. **The obvious call site is a problem and must be solved deliberately** — the harness's five `editor ·` guards assert the owner's real files are byte-identical across a run, and a migration that runs from an autoload would be writing into the real `user://` during every harness run. Either gate it off under the harness's own diversion, or give it a call site the harness does not reach. Until then the exe's identity is fixed where it has no such coupling (`export_presets.cfg` — Explorer, Properties, Task Manager) and the alt-tab label still reads the old string. **This is the second failure mode — two consumers of one value — and it cost nothing only because it was noticed within the hour.** |
| SG-210 | P1 | SHIPPING | **The executable identifies itself as a Godot port with no icon** | DONE 2026-08-11 (opus/coordinator), shipped in build 70 — evidence in SG-224; **reconciled 2026-08-11 by opus/coordinator, verified in code not asserted**: `assets/art/ui/icon.{png,ico}` both exist on disk and `export_presets.cfg` reads back `product_name="SkyGear"`, `file_description="SkyGear"`, `file_version="0.70.0.0"`, `company_name="Unconstrained"`. **The 256×256 art asset Alex owes is still open — see NEEDS_ALEX item 4**; this row covers only the identity fix | `export_presets.cfg:24` `application/icon=""`, `:27-28` both versions `"0.1.0.0"`, `:29` `company_name=""`, `:30` `product_name="SkyGear Godot Port"`, `:31` `file_description="SkyGear v11 Godot port - Milestone 1"`, `:32` `copyright=""` — all stamped into the exe by `:23 application/modify_resources=true`. `project.godot:7 config/name="SkyGear: Godot Port"`, and nothing calls `window_set_title`, so that IS the taskbar and alt-tab string. There is no `config/icon` and no `boot_splash` key, so window, taskbar and splash all carry the stock Godot logo. **No board row, doc or harness check has ever named any of this** — `docs/STEAM-LAUNCH.md` §4.3's icon rows are the Steamworks client icons, not the executable. This is the first thing a store-page visitor sees and it is the highest-ranked item in the audit. Files: `export_presets.cfg`, `project.godot`, one new 256×256 icon. **Gated on the icon being an art asset** — the Loom is not on this machine (SG-105) and asset specs are the owner's; ship everything except the icon key if he has not supplied one. Proof requires an exported build, which is REQUIRES-RUNTIME-EVIDENCE. **Pair with a new `shipping ·` check domain that parses `export_presets.cfg` and `project.godot` as text — the layer the player meets first is currently pinned by nothing, which is why this could exist behind a green 1187.** |
| SG-212 | P1 | AUDIO | **The first ten seconds of the game are silent, and one requested cue does not exist on disk** | **HALF DONE — STILL OPEN** 2026-08-11 (opus/coordinator); **reconciled by opus/coordinator, verified not asserted.** SHIPPED in build 70: the missing hover cue — `grep -rn card_hover scripts/` now returns only the comment at `hud.gd:50` recording the fix, `assets/audio/sfx/ui/hover.ogg` exists, and every menu hover has sound for the first time in the port. **NOT FIXED, WHICH IS WHY THIS ROW STAYS OPEN:** the headline defect, the first ten seconds of the game being silent, is untouched — `play_music` is still called exactly where it was | `audio.play_music(...)` has exactly one call site and it is inside `start_wave`; `audio.stop_music()` has exactly one, inside `go_to_title`. The game boots into `State.TITLE`, which no music call has ever reached — so title, Workshop, Berths, HOW TO PLAY, COMPARE and SETTINGS play nothing but `ui/click.ogg`. `assets/audio/sfx/world/` holds `amb_ship.ogg` and `amb_storm.ogg`, both imported, **both with zero references anywhere**. And `hud.gd:36` requests `"ui/card_hover.ogg"`, **which does not exist** (the directory holds `card_deal, card_pick, click, hover, slot_unlock`); `play_sfx` swallows the miss silently via `if not ResourceLoader.exists(full_path): return`, so every menu hover in the game has been mute for the entire life of the port with nothing able to see it. **The highest-value check in the slice is `audio · every cue the code asks for is on disk`** — walk every string literal passed to `play_sfx` and assert `ResourceLoader.exists`; it must be demonstrated failing on the current tree first. Files: `audio.gd`, `game.gd` (one call in `_set_state`), `hud.gd` (one string), `parity_test.gd`. **Do not bundle** `world/boiler_critical.ogg` never firing — that is an unbuilt feature, not this bug. |
| SG-213 | P1 | SHIPPING | **There is no demo build — the one export preset ships 100% of the game** | BLOCKED — needs the owner's scope decision; mirrored to `/NEEDS_ALEX.md` (SG-209 / DR-03) | `export_presets.cfg:1-13` is the only preset; `custom_features=""`, `exclude_filter="reference/*"`. `grep -rn -i "demo" scripts/` returns **zero** — no flag, no wave cap, no class lock, no end card — and `grep -rn "is_debug_build\|OS.has_feature" scripts/` returns zero, so there is no gating seam of any kind. Uploading today's exe as a demo hands over both heroes, the Colossus, the Workshop, Articles, fittings and, via SETTINGS → OPEN ALL HEATS, the entire Heat ladder. **The cut is already written but has never become work**: `docs/STEAM-LAUNCH.md:595` proposes *"waves 1–6, Captain only, Heat 0, no fittings/berths, results screen intact, and an end card that says what the full game has."* That is a standing recommendation, **not an accepted decision** — NEEDS_ALEX's only Steam line is *"start the paperwork whenever you want the clock running"*. Do not implement a cut the owner has not chosen. |
| SG-215 | P2 | UI | **The physical legibility floor is enforced only in a window, and the game ships fullscreen** | OPEN — filed 2026-08-11 (SG-209 / DR-05) | `project.godot:25-26` ships `window/size/mode=3` (borderless fullscreen) with `stretch/mode="canvas_items"`; `stretch/aspect` is set nowhere in the 98-line file. The only physical guard is `game.gd:528-530` `win.min_size = Vector2i(MIN_WINDOW_W, MIN_WINDOW_H)` — **a windowed constraint a fullscreen window does not satisfy**. `SkyGearInk.physical_pt` has no runtime caller at all, and the named check `ink · the point-size floor clears the physical floor at the min window` evaluates it only at 1600. **Every UI tool deliberately turns the shipped scaling OFF** (`tools/screen_shot.gd:70-76`, `tools/legibility_probe.gd:36-37`, `scripts/screen_poser.gd:102-103`), so the four-width matrix everyone cites as coverage has never exercised the shipped path. On a 1366×768 laptop, 12pt renders at 12 × 1366/1920 = **8.5 physical px against a `MIN_PHYS_PX` floor of 10.0**. **The ultrawide-distortion half of the original finding was struck**: Godot defaults `stretch/aspect` to `keep` and `tools/text_audit.gd:94-95` describes the shipped behaviour as a keep-aspect letterbox — a 21:9 monitor gets bars, not ellipses. Pin `aspect` explicitly as a drift guard, not as a distortion fix. Measuring this REQUIRES A RUNNING BUILD with content scale left on. |
| SG-216 | P2 | AUDIO | **A connecting hit makes no sound, and seventeen delivered takes are unreachable** | OPEN — filed 2026-08-11 (SG-209 / DR-06) | `damage_enemy` — the project's declared single funnel for damage dealt to a boarder — **contains no `play_sfx` call anywhere in its body**. `assets/audio/sfx/player/` ships `hit_1..hit_5`, `crit_1..crit_3` and `elem_ember/frost/arc/steam`, all imported; a repo-wide grep returns exactly one reader, `_: return "player/hit_1.ogg"` as the unknown-shape *cast* fallback in `_shape_sound`. **Seventeen** sibling takes are unreachable: `death_heavy_2`, `death_light_2/3`, `cannon_down_2/3`, `cannon_fire_2/3`, `cannon_hurt_2/3`, `crew_attack_2/3`, `crew_down_2`, `crate_break_2`, `hit_2..hit_5` — so all 21 gremlins in wave 11's opening batch die to one sample and every lane cannon fires one sample. **Corrections carried:** the swing does *not* fire on a whiff (`_process_basic_attack` early-returns when `nearest_enemy` is null), and hulk hits, prop breaks, kills and player-hurt all DO sound today. The gap is specific to **non-lethal hits on boarders**, plus crit and element having no acoustic signature. **Any take variation must ride `visual_rng`, never `rng`** — the seeded gameplay stream cannot shift. Files: `game.gd`, `parity_test.gd`. |
| SG-217 | P2 | BUG | **Nothing on a struck boarder changes — the hit flash and the status tint are both dead code** | OPEN — filed 2026-08-11 (SG-209 / DR-07); **argues against a recorded decision, see below** | `rig3d.gd:773 func react_hit(...)` exists and `rig3d.gd:27` advertises it; a repo-wide grep returns two lines — the definition and `view3d.gd:8796 _captain.react_hit(1.0)`. **No enemy rig is ever handed a hit reaction.** The flash is dead even for the captain: `rig3d.gd:859` uses `set_instance_shader_parameter("flash", ...)` and `view3d.gd:3674` states in the project's own words that this *"is a no-op against a StandardMaterial3D — it needs a shader that declares the uniform, which an imported glTF material does not"*; the only `.gdshader` in the project is the sky. **And the status tint is dead too** — `view3d.gd:8168-8176` writes burn/frost/stun colour to a `Sprite3D` billboard, while SG-89 records *"the deck is now ALL MESH"*, so no boarder billboard exists to tint. The renderer's own consolation, *"a number and a tint"*, is currently just a number. **THE PACKET MUST ACKNOWLEDGE SG-85 RATHER THAN PRESENT THIS AS PURE OVERSIGHT**: `view3d.gd:9174-9179` states the flinch rides `stun_time` because *"a flinch on every tick of damage would freeze a 180-hp wall solid"*. A scale/albedo tween is not a clip and sidesteps that, but the argument has to be made. **The dead status tint is the cleanest, least contentious half and could ship alone.** Whether a per-hit flash reads as impact or as a strobe is G5-class and the owner's. |
| SG-219 | P2 | ASSET | **All 121 textures ship unmipped, so the anisotropic filtering the project argues for does nothing** | OPEN — filed 2026-08-11 (SG-209 / DR-11); **needs no serialized-file lock** | Re-verified at synthesis: `find assets -name "*.png.import" \| wc -l` → **121**, with `compress/mode=0` × 121 and `mipmaps/generate=false` × 121. Against `project.godot:45-46`, which spends two comments arguing for anisotropy — *"The deck is one plane seen edge-on. Sixteen taps is the difference between plank detail and mush at the bow"* — and sets `anisotropic_filtering_level=4`. **Anisotropic filtering only operates on mipmapped textures, so that setting is doing nothing at all**, and every distant deck decal, prop plate and boarder texture shimmers as the camera sways. `export_presets.cfg:20 texture_format/s3tc_bptc=true` is inert while all 121 are mode 0. Files: `assets/models/**/*.png.import` and `assets/art/{props,enemies,ground,fx,env}/*.png.import` only — **leave `assets/art/ui/*` Lossless and unmipped, it is drawn in 2D**; set `compress/normal_map=1` on `*_normal.png`. No scenes, no scripts. **ARGUE THIS ON SHIMMER, NOT ON VRAM — the 4 GB-thrash claim was struck.** The measured pixel budget is ~135 MB for the whole 3D-facing set as RGBA8; compression saves ~100 MB against a ~700 MB figure dominated by 4× MSAA and shadow targets at 1440p, so **~14%, not a category change**. Re-import is a Godot operation and must not run beside another Godot process. |
| SG-220 | P2 | BUG | **A truncated `workshop.json` silently wipes every unlock, and the next run makes it permanent** | OPEN — filed 2026-08-11 (SG-209 / DR-14) | `workshop.gd:488-493` — `save_state` is open-WRITE + `store_string` + `close`: truncate-and-rewrite in place, **no temp file, no rename, no backup**. `:456-459` — any parse that is not a Dictionary returns `fresh()`, which is `unlocked:false, scrip:0, sigils:0, nodes:{}, articles:{}, fittings:{}, berths:[], best_heat:0` — everything the player has ever earned. The write fires from `bank()` on the frame VICTORY or GAMEOVER lands, and the **next** run's bank writes that fresh state over the corrupt file, ending recovery. **The project applies the opposite standard everywhere else and pins it**: `layout · a malformed screen entry falls back alone`, `view · a malformed lights entry falls back alone, never the deck`. No check anywhere loads a malformed workshop save. Two adjacent defects in the same file: `save_state`'s `bool` return is dropped by all four internal callers and all five UI call sites, so a denied write is completely silent (against `hud.gd:5504`'s *"COULD NOT WRITE THE RUN LOG — copy it before you leave"* on the same screen); and `respec()` indexes `NODES[id]` with **keys read out of the save file**, unguarded, so the first patch that renames a talent makes RESPEC raise, refund nothing and clear nothing. `fresh()` carries no `version` key, so there is nowhere to migrate from. **Note `fittings · a denied write reports clean without reaching the disk` tests the *ephemeral* early return, not the denial branch, which has no coverage at all.** **Honest severity:** the corruption trigger needs process death inside a ~1 KB write and there is no measured instance in this repo; it is filed high because the loss is total and silent, not because it is likely. A live sub-defect exists regardless — `hud.gd:4622-4629` computes the Workshop's COMMITTED ledger by walking `NODES.keys()`, so a stale node is already under-reported in the refund the screen promises. Files: `workshop.gd`, `parity_test.gd`. |
| SG-192 | P3 | DOCS | **~60 sentences across the docs claim the harness covers, pins or guards a behaviour without naming a check string — SG-11's own rule, violated repo-wide** | OPEN — found by opus/SG-191's documentation sweep, filed per rule 3, deliberately NOT fixed in bulk | **THE RULE IS BOARD RULE 2 AND IT HAS NEVER BEEN ENFORCED PAST THE FILES SG-11 ITSELF TOUCHED.** A full sweep of `STATUS.md`, `DESIGN.md`, `VFX-PLAN.md`, `README.md` and every `docs/*.md` (excluding this board and the night log) found violations in **fourteen files**. **Two distinct shapes.** *(a)* **No check at all** — e.g. `DESIGN.md` "Five checks guard all of it", `VFX-PLAN.md` "Fourteen checks, and every one of them asserts a **cap**", `docs/META-PROGRESSION-DESIGN.md` "two harness checks fail the build if a node or an article is ever added without a reader", `docs/DECK-IDENTITY-DESIGN.md` "that exemption is pinned by its own check", `docs/OUTSTANDING.md` "guarded so it cannot regress". *(b)* **A group PREFIX only** — `Four · edge checks`, `Nine · menu checks`, `Fifteen · crew checks`, `Seven · cloak checks`. Strictly these fail rule 2 as written, and the row filing this one thinks the rule should be AMENDED rather than the prose: a prefix plus a count is a pointer you can grep, which is most of what naming a string buys. **That is a decision, not a cleanup, and it is why this is filed instead of done.** **AND A THIRD SHAPE THAT IS WORSE THAN EITHER:** `docs/SHIP-AND-MAPS-DESIGN.md` cites **`parity_test.gd:1969`** — a FILE AND LINE NUMBER — as its evidence. A line number is the one pointer guaranteed to rot; it should be the check string. **WHAT SG-191 DID FIX, so this row is not re-doing it:** the five `editor ·` `user://` guards are now named in `STATUS.md`; `deck · the procedural deck obeys the same lamplit ceiling the models do` is named where the metalness ceiling is claimed; the stale CURRENT counts (README 774, `docs/STEAM-LAUNCH.md` 926, `DESIGN.md` 44) are corrected or dated. **Historical counts were deliberately left alone** — `372/372`, `946/946`, `725/729`, `517/517`, `463→478`, `596/596` are dated records of a moment and are not stale. **AND THE PRIOR ART IS ALREADY IN THE REPO, TWICE:** `docs/CLASS-2-DESIGN.md` carries six explicit `UNVERIFIED — no check` markings and is the model implementation; `docs/AUDIT-2026-07-31.md` §465 proposed the stronger rule **"no counts in prose"** a week ago and nothing adopted it. Deciding between "name the string" and "no counts in prose" is the first thing this row has to do. |
| SG-189 | P3 | BUG | **The crew's swing is still fitted to a countdown, and nothing has measured what they look like** | OPEN — filed by SG-188 per rule 3 | `scripts/view3d.gd` ≈L8130 passes `float(c.get("state_time", 0.0))` into `_sync_rig` as the attack clock, the same shape SG-188 fixed for boarders. The `attack_beat` parameter defaults to 0.0 and the call site passes nothing, so the crew fall through to `window = attack_clock` and are byte-for-byte what they were — deliberate, because changing how your own sailors swing is an owner-visible change and does not belong in a row about an enemy. SG-188's rate LATCH does already reach them, so they no longer accelerate; what is unfixed is that their clip is fitted to the remainder of their state rather than to the whole of it. **Nothing has measured them**: `tools/anim_timing.gd` has a captain path and a boarder path and no crew path, and `tools/swing_beat.gd` takes an enemy kind. The work is a crew beat (they have no `windup`/`recover` row in `SkyGearData.ENEMIES` — their timings live in `SkyGearLanes`), a way to measure it, and then the same one-line change. |
| SG-183 | P3 | BUG | **The pause and settings screens save `settings.cfg` to disk on every redraw** | OPEN — found by SG-181 | `scripts/hud.gd:3932` (pause) and `scripts/hud.gd:5283` (settings) are immediate-mode: they call `game.audio.set_volume(key, ui.slider(...))` unconditionally from the draw, and `set_volume()` ends in `save_settings()`. So the value is written back whether or not the slider moved, and a build left sitting on either screen rewrites `user://settings.cfg` every frame — which is what made SG-181's collision constant instead of rare, and it is a config-file write per frame in a shipped build for the whole time either screen is up. The fix is one comparison: only call `set_volume` when the slider's returned value differs from the one that was passed in (and the same for `toggle_mute`'s neighbours), which keeps the immediate-mode shape and makes the write an edge rather than a level. Cheap; kept out of SG-181 because it is a behavioural change to shipped screens and wants its own row. |
| SG-175 | P3 | BUG | **`tools/edge_ab.gd`'s noise floor is 0.00% at the mid pose and 0.17% at the STEM pose, and the tool's own rule says 0.00 is the pass condition** | OPEN — found by SG-174, filed per rule 3, deliberately not fixed | The floor is two plates with nothing changed between them, taken inside one `SkyGearStill.freeze`. At `mid` it is **0.00%** — measured on this session's code and on a stash of HEAD, so it is not SG-174's geometry. At `stem` it is **0.11–0.17%** across runs. The stem pose is the only one of the four whose frame is largely SKY, and `still.gd` stops `Engine.time_scale`, every `AnimationPlayer` and every `GPUParticles3D` — it says nothing about a shader clock or about the cloud and airship layer, which is the family of confound STATUS's fifth failure mode has now cost four times. **This did not invalidate SG-174**: the prow's own signal is 9.1% of the frame, 54x the floor, and the stern's is 0.000% at a pose whose floor IS 0.00%. But the rule in `still.gd`'s own header is that the floor must be exactly zero, and at one of the four poses this tool uses it is not. Wants: find which layer moves at the stem pose and either freeze it in `still.gd` or write down why it cannot be. |
| SG-171 | P3 | BUG | **`grip_at` is authored for all six weapons and NOTHING IN THE GAME READS IT** | OPEN — found by SG-170, filed per rule 3, deliberately not fixed | Every entry in the `weapons` block of `assets/models/weapons.json` carries `grip_at` — where along the shaft the hand belongs, 0 at the butt — taken from Alex's own modelling spec (`handoff-3d/weapons/PROMPTS.md:46`, *"a third up from the butt"*). `git grep grip_at` outside `reference/` returns that one prose line and nothing else: **no reader anywhere in the port**, which is STATUS's first failure mode sitting in the same file as the assets SG-170 is about. What consumes it instead is the hand-authored `offset`, which has to encode the slide along the shaft as a three-component vector in BONE axes and be re-derived from scratch every time the rotation moves. `tools/grip_sheet.gd --grip` gives it a reader IN THE FITTING TOOL and prints the offset that satisfies it (`_grip_offset`, the arithmetic is in its header) — that is where it was used to place all three SG-170 weapons. **It is deliberately NOT read by `SkyGearRig3D.hold()`**, because making the renderer apply a grip fraction would silently move the captain's cutlass, whose offset is Alex's own hand-fit and was not SG-170's to touch. The fix, if it is wanted, is a `grip_at` term inside `hold()` plus a one-time re-solve of every existing `offset` in the same commit so the diff shows the numbers changing together. |
| SG-164 | P2 | BUG | **A fire pool has an authored damage rate per source and the tick reads none of them — `dps` has never been read by anything.** | OPEN — filed by opus/SG-163, needs an owner decision, not a fix | Found while closing SG-163 and left alone on purpose: it is a **damage rate**, and the owner's instruction on the neighbouring bug was explicit about which side moves. Every pool in the game burns at a hardcoded **7.5 per tick** (`game.gd:_update_fire_fields`) regardless of what made it. Meanwhile the two sites that make pools each author a rate that nothing consumes: the Sear trail passes `"dps": spec.trail_dps` (**9.0**, `game_data.gd:278`) and the RESIDUE talent passes `"dps": 13.0 * mods.residue` (`game.gd:2961`) — STATUS's **first** failure mode, twice, in one dictionary. So the epic-rarity RESIDUE card currently buys **nothing at all**: its size term was the drawn radius, which SG-163 has just retired, and its damage term has never been read. `trail_radius` (46) in `game_data.gd` is dead for the same reason and is left in place rather than deleted so this row has something to point at. **THE QUESTION FOR THE OWNER IS NOT "should `dps` be wired up" but "should pools differ at all"** — one flat hazard is defensible and is what has actually shipped for the life of the port, and wiring three authored rates in would be three live balance changes bundled into a data cleanup. Whatever the answer, it belongs in `fire_pool_radius()`'s neighbourhood: SG-163 made that the one place a pool's size is decided and the rate should live beside it. Mirrored to `/NEEDS_ALEX.md`. |
| SG-161 | P3 | BUG | **The settings sheet prints "changes are saved when you leave this screen" straight THROUGH the BACK button, at every width, and the text audit is clean on it** | OPEN — found by SG-155, filed per rule 3, deliberately NOT fixed | `_draw_settings` puts BACK on the `y` cursor and the footer at `writing_area(sheet).end.y`; those two arithmetics have never agreed, and the footer lands on the button's lower third as a strikethrough. **PRE-EXISTING, PROVEN RATHER THAN ASSUMED:** shot at HEAD with `scripts/hud.gd` reverted, `.shots/screens/before/settings-1920x1080.png` against `.shots/screens/sg155/settings-1920x1080.png` — identical relationship, and SG-155's own change moves the button and the footer by the same 82 px so it is neutral on it. Reproduces at all four widths and mid-run. **THE INTERESTING HALF IS THE DETECTOR.** The audit renders 25 screens x 4 widths and reports CLEAN, so its OVERPRINT pass has no opinion about a free `_label` drawn over a `ui.button` — which is STATUS failure mode three's neighbour: not a detector silenced, a detector that was never pointed here. Fixing the two pixels without also asking why nothing saw them would be the smaller half of this row. NOT bundled into SG-155 because that is a feature commit and this is an unrelated layout defect on a shared file, which is the mixing SG-122 refused. |
| SG-153 | P3 | BUG | **A clean harness run raises 56 engine `ERROR:`s, all of them `Drawing is only allowed inside this node's `_draw()``** | OPEN - found while fixing SG-149, filed per rule 3, deliberately NOT fixed | Found the moment SG-149 gave the run a way to count its own errors. Every one is the same message and they come from the harness driving the WIDGET LAYER outside a draw pass: `scripts/ui.gd:247/251/254` (`button`) and `scripts/ink.gd:187/189` (`write`) paint when they are called, and checks call them for their RETURN values — focus, hit-testing, fitted size — with no `NOTIFICATION_DRAW` in flight. Backtraces name `tests/parity_test.gd:169` (26) and `tests/parity_test.gd:5061-5063` (`_view`, 12) among others. **The assertions themselves are sound** — they read what the widget returns, not what it painted — so this is noise rather than a wrong answer, which is why it is P3 and why SG-149 pinned it at 56 rather than gating it to zero and shipping a red harness. **But it is 56 raised errors in a log we now want to be able to read**, and each one abandons the rest of the painting function it was raised in, so any future check that DOES assert on what a widget drew would be asserting on a half-painted widget. The fix is likely a harness-side draw context (an offscreen `SubViewport`, or a `_draw`-scoped fixture node) rather than touching `ui.gd`. **When it is fixed, lower `ENGINE_ERROR_BUDGET` in `tests/parity_test.gd` and the check tightens itself.** |
| SG-154 | P3 | BUG | **A crit explosion re-hits the boarder that produced it — the browser's `skip:e`, dropped on the one call site where it still matters, and A HARNESS CHECK PINS THE DIVERGENCE** | OPEN — **but its RATIONALE was struck by SG-159 (2026-08-03) and it may not be fixed on the grounds written below.** The owner: *"I do not want you going back and trying to refer to the browser game."* Every argument in this row is browser fidelity. The self-hit may still be worth changing — a crit explosion hitting the boarder who produced it for an extra flat 20 is a design question — but it has to be argued and measured on how the Godot game plays, or dropped. Do NOT flip `crit · and the hit that opened it is still a crit` from 21.0 to 41.0 because a browser line says so | SG-148's row noted a dropped `skip:e` and asked what it did. **What it does is one thing and nothing else.** `reference/storm-dusk-v11.html:1832`, inside `damageArea`, reads `if (e === o.skip\|\|e.dead\|\|e.state === 'climb') continue;` — pure identity exclusion of a single boarder from the circle's target loop. Nothing else in that function reads `o.skip`. The browser passes it on BOTH explosions: the crit explosion (`v11:1734`, `{ noCrit:true, silent:true, skip:e, knock:60 }`) and the kill explosion (`v11:1863`, `{ skip:e, noCrit:true, knock:70 }`). `scripts/game.gd`'s `_damage_circle` has **no exclusion parameter at all** — its only filter is `not enemy.dead`. **IT IS A SECOND DIVERGENCE, BUT AT ONE SITE, NOT TWO, AND THAT ASYMMETRY IS THE FINDING.** On the KILL explosion it is reproduced BY ACCIDENT: `scripts/enemy.gd` sets `dead = true` *before* it calls `game.on_enemy_killed(self)` at all three of its call sites (lines 578-579, 589-590, 633-634), so the `not enemy.dead` filter already excludes exactly the boarder the browser names. Correct, and correct for a reason no one wrote down — if that ordering is ever changed, the kill explosion silently starts hitting its own corpse's slot. **On the CRIT explosion it is NOT reproduced**: a crit that does not KILL leaves the struck boarder alive, and the circle opened at his own feet hits him again for a flat 20 and 60 knock. The browser gives him 40; this port gives him 60. **AND THE HARNESS ALREADY ASSERTS THE PORT'S NUMBER.** `crit · and the hit that opened it is still a crit` (SG-147) computes `61 - 20 (the crit-doubled direct hit) - 20 (its own explosion) = 21` and its own comment names the self-hit out loud. The check's STATED purpose — that the opening blow still crits — is sound and worth keeping; its ARITHMETIC encodes the missing `skip`. Under the authored rule that boarder ends on **41.0, not 21.0**. This is the seventh failure mode in its mildest form: not a check written from a misunderstanding, but a check whose expected value quietly ratifies a divergence nobody had identified yet. **NOT FIXED HERE ON PURPOSE.** SG-148 is a measured balance change with a before-and-after; adding a `skip` parameter to `_damage_circle` is a second damage change, it would move the SG-148 arms underneath the measurement, and a fourth change riding on a tuning commit is what SG-122 refused. **THE FIX** is a `skip: SkyGearEnemy = null` on `_damage_circle` passed only from the crit-explosion edge, plus flipping that one expected value from 21.0 to 41.0 in the same commit so the diff shows the assertion changing sides — the shape SG-119 used when it inverted `telegraph · a boarder that draws no wedge keeps its circle`. Worth pairing with a check that the KILL explosion still skips its own corpse, since today that is an accident of statement order rather than a rule. |
| SG-151 | P3 | BUG | **The triangle budget is derived from an asset's HEIGHT, which understates anything wide and open-topped under a camera that looks DOWN** | OPEN — found by SG-150, filed per rule 3, NOT fixed | `tools/meshy.py`'s `tri_budget` is `0.021 × px²` where `px` is the asset's on-screen HEIGHT, floored at 3,000. That is right for the figures and props it was calibrated on, which are seen side-on and whose height is a fair proxy for their silhouette area. It is wrong for `stern_counter`: 190 × 151 × 178 ground units, an open box whose entire interior deck faces a camera pitched **41° down**. The law asks 3,000 for it; at 3,000 its planking goes blotchy and its hatch ring turns to mush at the real camera, while the bow — same width, same law, same 3,000 — is fine, because the bow is a closed hull seen mostly in profile. **The floor's own docstring already argues the right principle** ("what breaks at that point is not fidelity but FEATURES") and then measures it against height anyway. A fix would derive the budget from PROJECTED AREA at the shipped camera rather than from height, or simply let a piece carry a `tris` override the way the gunner drone does. Not fixed here because it moves the budget for every asset in the manifest and wants its own before-and-after. Evidence: `.shots/sg148/compare/stern_counter_t3000-y035.png` against `stern_counter_t8000-y035.png`. |
| SG-152 | P3 | INFRA | **Nothing in the harness guards the triangle ceiling, and the `still` readback check can be satisfied by a COMMENT** | OPEN — found by SG-150, filed per rule 3, NOT fixed (`tests/parity_test.gd` is held by another agent) | **(a) No triangle guard exists.** SG-145 left three pieces out of `static_model.gd` on the triangle law, and nothing in the harness would have caught them being wired anyway; SG-150 has now brought them under it, and nothing would catch a regression either. The check to add: for every key in `static_model.gd`'s `MODELS`, load `assets/models/<key>/<key>.glb`, sum `surface_get_arrays(...)[ARRAY_INDEX].size() / 3` across surfaces, and assert it is `<= 8000` (`meshy.py`'s `TRI_CEIL`) — named something like `prop · every wrapped static model is inside the triangle ceiling`. It fails on the old state by construction: at HEAD~ those same three keys measured 10,312 / 30,666 / 30,410. **(b) The readback check is a substring test over the whole source**, so a tool that merely MENTIONS `RenderingServer.frame_post_draw` in a comment passes `still · and every frozen readback waits for the renderer, not for the tree` without ever calling it. Found accidentally while demonstrating that the check fails: stripping the `await` but leaving the header comment left it GREEN, and only stripping the comment as well turned it red. The `STILL: NOT APPLICABLE` marker has the same shape, but that one is a deliberate, documented declaration and this is not. Stripping comments first — which the sibling check at `parity_test.gd` ~line 338 already does, for its own reason — would close it. |
| SG-125 | P2 | MEASURE | **SG-48's stowage cut is still the right call, but not for the reason written down** — its evidence was gathered where the effect could not appear | OPEN — verify-and-annotate, NOT a request to resurrect the feature | SG-48 CUT the stowage variety spine on its pre-committed kill-test: close-share **live 5.38% (sd 2.20) vs flat 5.25% (sd 1.72)**, mean diff 0.13 ± 0.36 (t≈0.36) over 120 runs, read as "combat statistically identical". **SG-118 then established that `tools/balance.gd` never moved its bot**, and that a moving captain spends **~22%** of a run at close range against the stationary bot's **3–5%**. So the statistic that cut the feature was measured in a regime where the thing the feature acts on barely occurred: a 0.13-point difference inside a 5% band, produced by an instrument that spent ~95% of every run outside the range where the effect lives. **That is not evidence of no effect; it is an instrument that could not have seen one.** **Do NOT resurrect the feature.** It is independently superseded by the owner's own reframing — variety belongs to ENEMIES within a run, and ship modification happens BETWEEN runs — which is a perfectly good reason to have cut it, just a different one from the one recorded. This row exists so the repository stops claiming an evidential basis it does not have. **Action:** annotate SG-48's row to say the cut now rests on the design reframing, and either re-run its statistic on the repaired rig or state plainly that it was never re-run. Either is honest; the current text is not. This is the fifth failure mode reaching backwards — every verdict SG-48's family produced is in the same position, and this row is the audit of which ones. | **AUDIT DONE 2026-08-03 (opus, read-only).** Seven rows rest on this rig. **SG-48** — evidence void, cut stands on the owner's reframing (this row). **SG-57** — held-rate flipped sign on the repaired rig and lost significance; it was a bot fact; the owner's threshold question is retired. **SG-70 — THE ONE THAT WAS COSTING THE OWNER A PLAYTEST.** Its row is OPEN pending *"the owner's next playtest"* and explains its 25%-vs-58% ally-share gap by saying the bot "spends 55–60% of the fight at long range" — which is the stationary-bot artefact exactly. Close-range time is 3–5% stationary against ~22% moving. **Re-measure on the repaired rig BEFORE spending a playtest**; if it now reads ~58% the mystery closes and SG-62's fix can finally be judged. **SG-97** — already discounted its own numbers as noise for the right reason; conclusion stands. **SG-62** — deferred to SG-70. **SG-14, SG-26** — **EXAMINED 2026-08-03 (opus/SG-70), and the audit is now CLOSED at nine rows.** Neither is a mere capability-add: both RECORDED measurements from the stationary rig, so both belong on the affected list. But in both the balance figures sit in a single separable paragraph, and **everything else in each row is carried by checks that never step a captain and therefore still stands** — SG-14's six `heat ·` checks (data tables, `begin_run`/`open_draft`; cumulative stack, readers, the three rung effects, the summit sigil, the ladder UI, and monotonicity guaranteed by the `HEAT_HARDER` direction table rather than by measurement) and SG-26's eleven `shop ·` checks (32-cell matrix, reroll refused, capacity 5, the fifth shape dealt in the first card's place, the exclusion pair, the sigil economy). **So: not fine, but not broken either — each needs one paragraph struck, not a rebuild, and that is the shorter answer the audit was hoping for.** Both paragraphs are now annotated in place and the re-measure is SG-127. **AND THE AUDIT FOUND ONE THING THAT NEEDED NO RE-RUN AT ALL:** SG-14 records Heat 0 as **5/6 held** and SG-26 records Heat 0 as **3/6 held** — same tool, same Heat, same seed list, two answers. The rig's own header now declares an **8% noise floor**; n=6 could never have supported either number, and the disagreement was sitting in the ledger the whole time. **The finding that outlived the audit:** SG-97's row had already WRITTEN DOWN that the bot never moves, a day before SG-118 rediscovered it. The fact reached no other row and not the tool's own comment. That is now the sixth entry in STATUS's failure-mode list, and a seventh went in beside it from SG-119 — a harness check that asserts the bug.
| SG-121 | P3 | BUG | **Three fire-field creators write a `radius` and a `dps` that nothing reads** — the sixth instance of the project's first failure mode | OPEN — found by opus/SG-117's damage-source sweep, filed per rule 3, NOT fixed | `_update_fire_fields` (`game.gd:3923+`) hardcodes `78.0` and `7.5` for enemies and `78.0` / `3.0` for the player. All three callers of `_field` write per-field numbers it never consults: RESIDUE (`game.gd:2926`, `radius 62+22*residue`, `dps 13*residue`), the Boilerwright's **bleed-jet scald trail** (`game.gd:3367`, `trail_radius 46.0`, `trail_dps 9.0` from `game_data.gd:219`), and the broken lantern (`game.gd:3895`, which writes neither). So a scald trail is the same 78-unit pool at the same rate as a lantern fire, the RESIDUE card's whole scaling term does nothing, and the Boilerwright walking his own retreat trail eats the identical tick. STATUS names *"data with no reader"* as the failure mode that has happened FIVE times and says to extend the two existing guards rather than trust yourself; this is the sixth. Deliberately NOT fixed here: making those fields live changes fire damage a second time in the same session, and SG-117 already moves it. — **2026-08-03 (opus/SG-136): THIS ROW IS WRONG ABOUT `radius`, AND THE TRUTH IS A WORSE BUG THAN THE ONE FILED. STILL OPEN, AND STILL NOT FIXED, BECAUSE BOTH OF THE TWO OPTIONS IT OFFERS CHANGE THE GAME.** `radius` is not data with no reader. **`view3d.gd:4470` reads it** — `var fr: float = float(f.get("radius", 62.0)) * 2.2` sizes the fire decal, the scorch decal under it and the permanent `MarkKind.SCORCH` burn. So the fields are read by the RENDERER and ignored by the DAMAGE, which makes this the project's SECOND failure mode (two functions disagreeing about one number), not its first — and the disagreement is **the fire you can see is not the fire that burns you**, in all three creators and in both directions: the Boilerwright's scald trail is **drawn at 46 and burns at 78**, a hitbox 70% wider than its own telegraph; the broken lantern is **drawn at 62** (the renderer's fallback, since it writes no radius) **and burns at 78**; RESIDUE at one stack is **drawn at 84 and burns at 78**, so its card's whole scaling term moves the picture and nothing else. Standing where no fire is drawn costs the captain 12 dps in two of the three. **`dps` alone is genuinely unread** (`_update_fire_fields` hardcodes 7.5 to enemies and `damage_player(3.0)` to the captain). **WHY NEITHER OPTION WAS TAKEN:** wiring `radius` moves three hitboxes (two shrink, one grows); wiring `dps` moves fire's damage rate a third time in one session, on top of SG-117 and SG-122; and DELETING `radius` is not behaviour-preserving either, because the renderer would fall back to 62 and every drawn fire on the deck would change size. There is no version of "wire it or delete it" here that a agent may take silently — it is in `/NEEDS_ALEX.md` with the three numbers. No harness check was added: the only check that could be written today is `hazard · the fire you can see and the fire that burns you are the same circle`, and it FAILS at HEAD, so shipping it would ship a red harness. |
| SG-127 | P2 | MEASURE | **SG-14's difficulty claim and SG-26's two Article verdicts are unevidenced — both balance paragraphs were measured on the stationary bot** | OPEN — filed 2026-08-03 by opus/SG-70 closing SG-125's audit, annotated in place, NOT re-run | The last two rows of SG-125's audit. Neither row is broken — every structural claim in both is carried by checks that never step a captain (SG-14's six `heat ·`, SG-26's eleven `shop ·`) and those stand. **What is void is one paragraph in each.** SG-14: "Heat 0 held 5/6 avg wave 11.7 allies 34% · Heat 3 held 0/6 · Heat 5 dead on wave 4 every run, allies 4%", and the conclusion **"graded rather than a cliff"**. SG-26: "baseline 3/6 · opening_bid 6/6 wave 12.0 · second_hand 1/6 wave 10.8, passives 4.5% vs 3.2%", and the conclusions **"the named-kit benefit is real"** and **"the trade is real"**. **THE TWO THINGS WHOEVER TAKES THIS MUST NOT REPEAT.** (1) **n=6 is not a measurement on this rig.** These two rows disagree with each other about the same Heat 0 baseline — 5/6 against 3/6 — which is the cleanest available proof that the sample was too small, and the header now declares an 8% noise floor on damage-taken. Use `-- 6 <heat> <article> <reps>` and pool; SG-70 used n=30 per arm and SG-118's reference batches were n=120. (2) **Watch the CEILING.** Heat 0 now holds ~92%, so a hold-count comparison at Heat 0 is nearly saturated and will read "no difference" for reasons that have nothing to do with the thing under test — the same shape of error SG-125 was created to catch. Read wave-reached and damage-taken, or run the Article arms at a Heat where the hold rate is not pinned. **Do not resurrect or retune anything from the result** — SG-14's rungs and SG-26's Articles are both shipping and both mechanically evidenced; this is about what the ledger is entitled to CLAIM.. **✅ DONE 2026-08-03 (opus/SG-128). All three claims re-measured on the repaired rig; ONE REFUTED, ONE NOT ESTABLISHED, ONE RELOCATED — and none was rescued.** **(1) "Graded rather than a cliff" (SG-14) — REFUTED.** Heat 0 n=240 held 86.7%, Heat 3 n=120 66.7%, Heat 4 n=120 77.0%, Heat 5 n=120 **0 of 120**, dead on wave 4.02 with sd 0.18. Rungs 0-4 sit in a twenty-point band; Heat 5 is a wall, not a further step. **HEATS 1 AND 2 WERE NOT ATTEMPTED** — machine time went to the Article arms — so that curve has two rungs missing and must not be read as complete; the cliff is measured rather than interpolated, so the gap does not rescue the claim. Heat 4 above Heat 3 is **not** claimed as a re-ordering (Wilson 65.1-85.8 vs 57.8-74.5, overlapping). Owner decision in NEEDS_ALEX §SG-127. **(2) "The named-kit benefit is real" — NOT ESTABLISHED**, claim as recorded refuted, and the arm is confounded by a bot-policy difference (SG-130). **(3) "The trade is real" — SURVIVES ON NEW EVIDENCE**, +20.4% damage taken at p=0.00003, while the hold-rate cost it was actually recorded on is null. **BOTH OF SG-127's WARNINGS WERE HEEDED**: n=120 not n=6, and Heat 3 rather than the saturated Heat 0. **Pre-registered before any arm ran**, all three outcomes fixed in advance including the wording of a null, and the analysis reports Bonferroni over all six tests rather than the best of them. Rows annotated in place; nothing deleted; nothing tuned. |
| SG-124 | P2 | BUG | **`marks_shot.gd`'s "worst honest case" never puts a mark in the ring it measures** — it reports 0.0% overlap and its own header says that makes the figure a coincidence | OPEN — found by SG-116 while re-measuring on the corrected mask, filed per rule 3, NOT fixed | The deck-marks cost is `+0.02%` against a 3% gate, and it is `+0.02%` in BOTH passes — the wave-12 distribution AND the "worst honest case" that exists specifically to force an overlap by stamping the deepest mark this system can make directly under each rune. Both passes print **`of that ring, the marks darken 0.0%`**, and the tool's own next line says *"if this is ~0 the figure above is a coincidence"*. It is. **The marks are rendering and the stamp does work** — `marked-worst-tele.png` and `marked-telegraphs-tele.png` are different files and both differ from `clean-worst.png`, so this is not SG-101's "the mark is under the boarder's mesh" scar returning. The marks land somewhere; they do not land in the **12 px `RING_PX` band around the rune**, which is the only planking this measurement looks at. The likely cause is now visible for the first time: the worst case stamps at `t.at.lerp(captain, 0.3/0.5/0.7)`, but the melee rune is a wedge CENTRED on the boarder with radius `reach`, so those three points can sit well beyond the wedge and its 12 px ring. Until a mark is demonstrably inside the ring, **the deck-marks figure is evidence that marks and runes do not overlap in this pose, and not evidence that marks are cheap** — which matters because `MARK_CAP` 48→24 and `MARK_ALPHA_MAX` 0.30→0.12 were already retuned once on a number from this tool (SG-115). Fix: stamp inside the measured ring — `SkyGearRune.save_mask()` now writes the rune pixel set as a PNG, so where the ring actually is can be seen rather than reasoned about.. **⚠ MEASURED 2026-08-03 (opus/SG-128) — CONFIRMED, AND THE CAUSE IS WORSE THAN FILED: THE WORST CASE IS NON-DETERMINISTIC.** Five runs on unchanged code: new marks placed 0/13/0/0 of 60, ring darkened 0.2% / **14.0%** / 0.0% / 0.0% / 0.0%, cost +0.01% / **+1.33%** / +0.00% / +0.09% / +0.07%. **Four runs in five, not one of the sixty stamps reaches the planking**, so the 0.0% this row filed is the COMMON case rather than a one-off, and the tool's coincidence warning is firing correctly. **The cause is not the lerp geometry this row guessed at.** `_mark` has three outcomes and only one places a disc: inside `MARK_MIN_SEP` of a same-kind mark it DEEPENS that one at ITS position, and with the cap full — which it always is, because `_mark_the_deck` fills all 24 slots first — it retires a victim and queues the newcomer in `_mark_pending`. Whether that retirement completes depends on mark AGES, which depend on how the awaited real frames fell. Same code, same seed, sometimes 13 marks and sometimes zero: SG-108's family again, a scene less still than the tool believed. **FIXED IN THE INSTRUMENT, NOT IN THE GAME:** the tool now snapshots the slot state and prints how many stamps took a NEW slot, how many merely deepened, how many are pending, and how many of the twelve requested positions actually carry a mark — and says in words that a zero-fresh pass is the first pass photographed twice. **THE EVIDENTIAL QUESTION SG-128 WAS ASKED (does the `MARK_CAP` 48→24 / alpha 0.30→0.12 retune rest on anything?): THE CONTRAST LEG RESTS ON NOTHING, IN EITHER DIRECTION.** The 11.5%/9.1% pair is not reproducible from this repository (SG-115) and `view3d.gd`'s own comment already says so in writing; the current tool is inert four times in five. The one run where it worked reads **+1.33% against a 3% gate** — under it — and that is n=1 and is NOT claimed. **The COVERAGE leg IS reproducible**: 5.7% painted at cap 24, verified. **`MARK_CAP` AND `MARK_ALPHA_MAX` ARE DELIBERATELY UNCHANGED IN BOTH DIRECTIONS** — the owner has the marks on his play list to judge by eye, and the honest outcome here is that this number never meant anything, which makes his eye the deciding instrument rather than a competitor to one. |
| SG-105 | P3 | ASSET | Four Aether Loom prompts specced for the character cluster; the Loom is not running on this machine | BLOCKED — needs the owner to start the Loom | `curl http://127.0.0.1:8765/api/config` refuses the connection and `ASSET-GENERATION.md`'s install path (`C:/Users/alexr/Documents/Codex/2026-07-26/done-66-images-fully-specced-for`) does not exist, so `run_aether_loom.ps1` could not be found to start it. **Nothing was faked**: SG-103 ships on art already in `assets/art/ui/`, exactly as the menu rebuild did. The four prompts are written out in the house frames (`PLATE` / `ICON` / `BAND` from `tools/forge.py`, verbatim) in `docs/HUD-DESIGN.md` §6. In priority order: **`portrait_boilerwright.png`** — a real bug, he wears the Corsair's face today; **`bezel_porthole.png`** — a heavy cast rim with its rivets in the casting, replacing the code-drawn one; **`bar_housing_slim.png`** — an 8:1 trough so the light gauge can be painted too; **`bar_fill_*_v2`** — optional, since SG-104 fixed the code rather than the art. |
| SG-73 | P3 | DECISION | The Colossus wreck fitting is a 210-unit painted card off the bow — migrate, or bless the billboard as the landmark style | OPEN | Surfaced by SG-64's inventory sweep — the largest painted object the deck ever shows once the mast went 3D. It is DELIBERATELY out of the purge's scope: owner-placed trophy art (SG-15/SG-56), off the play envelope at (0, -1500), read as a distant silhouette where a billboard is the cheapest honest answer — but it fills the top of frame at the bow (.shots/sg64/ballista-after.png shows the read). Needs the owner's taste call, not a default generation. |
| SG-7 | P2 | DECISION | Boilerwright mobility: compensate him somewhere feelable, or make the comparison screen say the trade plainly | BLOCKED — needs owner | Measured 60% gap (he covers 40% of her ground) after SG-1 made the measurement deterministic — was recorded as 67%/33% off a noisy sample. Explicitly not a tuning-by-feel item. |
| SG-20 | P3 | ASSET | Captain's sword grip re-fit in the lab with the timeline running | OPEN | "Two minutes in the fitter." The −120° pitch was tuned at rest; check it through `swing2`. |
| SG-24 | P3 | BUG | Popup menus reported drifting right — not reproduced | OPEN | Measured static across eight samples. Needs to know WHICH menu before it can be chased. Do not close: unreproduced ≠ absent. **RE-CHECKED 2026-08-02 (opus/small-bugs), after a day that changed the renderer a great deal — new mesh figures, the SG-81 model lights, new particle work. STILL NOT REPRODUCED, and the row STAYS OPEN: unreproduced is not absent.** `tools/text_audit.gd`'s drift pass (`_drift`: hold a pose, redraw 60 times, compare every declared widget rectangle by index) was run windowed over all 24 posed screens at all four sizes — every menu, sheet and end screen the game has, PLAY excluded because its readouts are supposed to move. Zero DRIFT findings at every width, and the identical run against a clean worktree at HEAD reports the same. It still needs the owner to name WHICH menu before it can be chased. |
| SG-93 | P2 | UI | Spread the SG-91 menu vocabulary to SETTINGS, HOW TO PLAY, CONTROLS and PAUSE | BLOCKED — needs the owner's verdict on SG-91 first (it is in NEEDS_ALEX) | `docs/MENU-DESIGN.md` §5 is the ordered plan and §3 is the vocabulary. DELIBERATELY not done in the same pass: the owner asked to see the direction before it spreads, and the four screens are also a bigger blast radius than the title was. The title's plates are `bare` widgets drawn by `_menu_plate` in `hud.gd`, so `ui.gd` was never touched; those four screens are `ui.row` / `ui.slider` columns — a LEFT label with a RIGHT value, which is a different widget from a centred plate and wants its own noun (an engraved ledger line on the same brass) rather than a reuse of this one. Doing it through `SkyGearUI` would also change the pause sheet, the keys sheet and the Workshop's BACK button in the same edit. Judge 2 and 3 in `MENU-DESIGN.md` §5 before touching the run report. **Not the fight HUD** — different rules, and §5 says why. |
| SG-95 | P2 | ASSET | **Dynamic shadows across the board — "not one circle blob under everything"** | OPEN — the owner's own aesthetic ask, build-44 playtest item 13 | Distinct from SG-94, which fixed ONE figure's contact shadow because it was a reported bug: the Colossus is thirteen parts and its single figure-shaped blob hung in the air detached from them. SG-94 deliberately did NOT redesign the shared system — every other figure, prop, crewman, projectile and pickup still wears the same `_blob_texture` ellipse scaled off its gameplay radius, drawn through one MultiMesh (`view3d._build_shadows`, cap 256). What this row wants is different in kind: the deck already has ONE shadow-casting directional light (`moon`, `directional_shadow_max_distance` 34, opacity 0.72) and every figure mesh casts into it, so real shadows partly exist and the blobs are laid on top. The job is to decide what the blob is FOR once the cast shadow is real — contact darkening only, or gone — and to pay for casting at the lamps rather than only the moon. SG-94 left two things it can build on: `_shadow` now takes a per-axis depth (opt-in, defaulting to the old 0.62 squash) and `_part_shadows` is a worked example of a shadow derived from a mesh's own measured footprint rather than from a gameplay number. |
| SG-132 | P2 | MEASURE | **Held-rate is running out of room at Heat 3 — the rung is turning into the saturated ceiling SG-127 warned about at Heat 0** | OPEN — filed 2026-08-03 by opus/SG-130 per rule 3, NOT fixed | Found while re-running SG-127's Article arm on the SG-130-repaired bot. **The repaired bot no longer spends draft slots on passives, so the Heat 3 BASELINE rose from SG-128's 75.0% held to 80.8% (97/120).** The two agree inside the +-9-point resolution, so this is NOT a contradiction — it is the same rung measured with a slightly better captain. **The consequence is what matters: 80.8% leaves 19.2 points of headroom, and n=120 needs +14.5 points under the Bonferroni(6) bar to establish anything.** So the entire establishable range at this rung is roughly **+14.5 to +19.2 points** — a positive effect must be nearly the whole of the remaining room before this rig can see it, which is exactly the shape of error SG-125 was created to catch and SG-127 warned about one rung lower. **ANYONE MEASURING A POSITIVE EFFECT AT HEAT 3 FROM HERE NEEDS EITHER A HARDER RUNG OR A METRIC WITH ROOM.** Wave-reached and damage-taken both still have range (this session read them at +2.7% and +11.0%; damage-taken cv 0.43, 15.6% resolvable at n=120). Heats 1 and 2 have still never been run — SG-128 records that gap and it is unchanged. **Not a bug and nothing in the game to fix**; it is a fact about what the instrument can still see, filed so the next three agents do not each rediscover it one at a time. Evidence: SG-130's Done row and `docs/NIGHT-LOG-2026-08-03.md`. |
| SG-136 | P2 | BUG | **`--fixed-fps` makes `lit_probe.gd` repeatable and NOBODY IN THIS REPO IS USING IT — but adopting it re-poses the deck, so the tool's published numbers are a rebaseline decision** | OPEN — filed by opus/SG-136 while working SG-133, per rule 3, NOT fixed | SG-133 asks for a fixed-delta settle and neither hand-rolled version of one works (see that row). Godot already ships the lever: **`--fixed-fps 60` forces a fixed delta on the WHOLE engine** — every `_process`, every `_physics_process`, every self-driving node — with no code change at all. Three runs of **unmodified HEAD** with the flag, against three without, off the same clean extracted worktree and the same import: whole-frame boarder pixels **78077 / 78108 / 78084 (0.04%)** against **99448 / 100066 / 99923 (0.62%)**; molten fraction **0.81% / 0.81% / 0.81% — bit-identical** against 0.24 / 0.23 / 0.25; figure/deck **1.016 / 1.023 / 1.009 (1.4%)** against 1.023 / 1.062 / 1.057 (3.8%). So the flag removes the whole of SG-131's residual and three quarters of the ratio's. **AND IT IS NOT A DROP-IN, WHICH IS THE HALF THAT NEEDS A DECISION.** The flag governs the tool's SETUP too, so the sim reaches the settle having advanced a different amount and the wave spawns more: **8 rigs and 10 AnimationPlayers with the flag, 3 and 4 without.** A different number of boarders in different facings is a different photograph, and it is what moves the molten fraction 0.24% -> 0.81% — so `MOLTEN_FLOOR = 0.18`, `PAINTING_MOLTEN_FRONT = 1.25` and SG-131's published 0.13-0.15 / 0.22-0.24 bands were all read off a composition that the clock chose. **AND THAT IS THE FINDING UNDER THE FINDING: `lit_probe.gd`'s scene is not pinned by construction. The number of boarders it photographs is a function of how fast the machine ran.** It happened to be 3 on three real-time runs here and on the three the SG-133 row recorded, so it has never been caught. Re-baselining those constants is changing tuning values to make a result look better, which this session's standing rule forbids doing silently — so it is in `/NEEDS_ALEX.md`. **RECOMMENDATION:** adopt `--fixed-fps 60`, then pin the roster with a check that reads the count rather than trusting it (`lit · and the deck holds the boarders this tool posed, not however many the clock spawned`), then re-measure the molten bands against the pinned scene and republish them as one edit with the before/after in the row. Files: `tests/lit_probe.gd`, `tools/still.gd`. |
| SG-143 | P3 | BUG | **A destroyed deck cannon is a flat painted wreck that never fades either** | OPEN — found by the SG-138 flat-draw audit, filed per rule 3 | The live gun is the `cannon_deck` mesh; the moment it dies the renderer swaps to a `Sprite3D` of a painted wreck at 130 units and leaves it there for the rest of the wave — so the FLAT version of the object is on screen strictly LONGER than the mesh it replaces, in three fixed mid-frame lane positions. There is no `cannon_deck_destroyed` model anywhere. **This is also the answer to "what else large is exempt from SG-139's fade": this one, and only this one.** Two candidate fixes, cheapest first: tint/damage the existing mesh the way `_sync_boiler_damage` tints the Boiler, or commission the wrecked state. |

---

## Resolved rows live in `docs/BOARD-ARCHIVE.md`

This file is the **open queue and nothing else**. Every DONE, CUT, DROPPED,
CLOSED, RESOLVED, SUPERSEDED, CORRECTED and ANSWERED row — 190 of them — is in
`docs/BOARD-ARCHIVE.md` with its evidence intact. Search there for any ID you
cannot find above.
