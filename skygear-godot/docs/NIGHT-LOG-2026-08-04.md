# Night log — 2026-08-04

Evidence that does not fit in a board cell. Rows carry the argument; this file
carries the raw tool output the argument was read off, so the next person can
check the arithmetic rather than take it.

---

## SG-193 / SG-194 — the owner's build-62 sentence, both halves

> *"Do enemies fight and kill crew members? Furnace knights dont seem to do any
> damage to me or attack crew members."*

Two questions, one cause. The crew half is false and the captain half is true.

### `tools/crew_probe.gd` — 12 seeds, Heat 0, full twelve-wave runs

Two arms. `gunsdown` destroys the three lane cannons on every frame, which is
the direct test of "the cannon outranks the crew".

```
  CREW PROBE · 12 seeds · HEAT 0 · step 0.0167 s (the engine's own, not one this rig asked for)

  --- SHIPPED -------------------------------------------------
      [rig] ground covered / (|v| x step) = 1.0110 — the two clocks are one clock
      swings resolved   4062 over 12 runs
      AIMED AT         captain   0.2%   cannon  50.9%   CREWMAN  36.6%   boiler  12.3%
      LANDED ON        captain   0.0%   cannon  50.9%   CREWMAN  34.0%   boiler  11.0%   swallowed   4.1%
      crew              47.4 mustered per run,  24.4 of them hurt, 21.58 KILLED
      crew HP lost      1714.2 per run  (a hand is 68 HP, so 25.2 hands' worth)
      when hurt he was   537 units from his own station   (the assist's reach is 560)
      stomps resolved    3.3 per run
      per seed · crew killed      mean   21.6  sd  13.7  95%   13.9..29.3   [44 15 11 17 44 30 32 0 22 10 12 22]
      per seed · % swings at crew mean   32.9  sd  15.1  95%   24.4..41.5   [45 29 27 27 62 42 41 1 44 21 28 29]

  --- GUNSDOWN -------------------------------------------------
      [rig] ground covered / (|v| x step) = 0.9878 — the two clocks are one clock
      swings resolved   5613 over 12 runs
      AIMED AT         captain   1.8%   cannon   0.0%   CREWMAN  89.1%   boiler   9.1%
      LANDED ON        captain   0.0%   cannon   0.0%   CREWMAN  86.8%   boiler   6.8%   swallowed   6.5%
      crew              86.1 mustered per run,  75.2 of them hurt, 71.42 KILLED
      crew HP lost      5491.2 per run  (a hand is 68 HP, so 80.8 hands' worth)
      when hurt he was   256 units from his own station
      stomps resolved    4.4 per run
      per seed · crew killed      mean   71.4  sd  21.4  95%   59.3..83.6   [94 43 84 79 24 60 97 84 71 88 64 69]
      per seed · % swings at crew mean   88.9  sd   8.8  95%   83.9..93.8   [92 76 96 88 70 85 97 97 96 92 83 96]
```

The two `crew killed` intervals — 13.9..29.3 and 59.3..83.6 — do not touch, and
neither do the two share intervals. The mechanism the dispatching read predicted
is real; its predicted magnitude ("crew are almost never targeted") is wrong by
about two hundred times.

Note the per-seed zero in the shipped arm (`CRW8`, 0 killed / 1% of swings). One
run in twelve keeps its guns up all the way and the crew are never touched. That
is the shape of the effect, not noise around it.

### `tools/melee_probe.gd` — 30 s × 12 reps, Heat 0, on the corrected clock

```
  ARMORED  stand   resolved 180  aimed 180  LANDED 180  swallowed 0  elsewhere 0   taken 6120.0   dist 105.7  off-axis 0.5 deg
                   CLOSED to    93 at best (a swing trips at 99), mean gap 106, 15.1% of the window inside it
  ARMORED  crowd   resolved 180  aimed 180  LANDED 180  swallowed 0  elsewhere 0   taken 6120.0   dist 105.7  off-axis 0.5 deg
                   CLOSED to    93 at best (a swing trips at 99), mean gap 106, 15.2% of the window inside it
  ARMORED  orbit   resolved   0  aimed   0  LANDED   0  swallowed 0  elsewhere 0   taken    0.0
                   CLOSED to   100 at best (a swing trips at 99), mean gap 118,  0.0% of the window inside it
  ARMORED  lethal  resolved  24  aimed  24  LANDED  24  swallowed 0  elsewhere 0   taken  850.0   dist 104.3  off-axis 2.1 deg
                   CLOSED to    92 at best (a swing trips at 99), mean gap  95, 22.4% of the window inside it

  ARMORED   wave 11 · 576 effective HP · against a captain with 120 HP
            LIVED  5.33s (sd 0.81, n=11) on average, and in that whole life
            it resolved 2.2 swings and LANDED 2.2 — and dealt 77 damage
            (sd  30.8) BY ITS OWN HAND, 64% of that captain's pool.
  per rep · seconds  5.30 6.00 4.80 4.80 5.60 5.65 6.45 5.80 4.40 6.05 3.75
  per rep · damage     68   68   34   68  102   68  102  136   68  102   34

  ARMORED  stand   took 6120.0 in 360.0s = 17.00 dps  ->  a full captain falls in 5.9s   (180 of 180 landed)
  ARMORED  orbit   took    0.0 in 360.0s =  0.00 dps                                     (0 of 0 landed)
```

**Closed to 100 against a trip of 99.** Six minutes, one unit short. `bot.gd`
dashes to break contact at `DASH_AT 90`, so the bot's floor sits just outside the
knight's reach — which is why the zero is clean, and why it is the bot's zero
rather than the game's.

### What SG-165 recorded, and why it is withdrawn

SG-165's ORBIT result — *"0 resolved swings in 24 measured minutes"* before the
health buff, *"10 resolved and 9 landed"* after — was taken before SG-190. On the
corrected clock ORBIT is **0** at hp 360. The buff bought him life (5.33 s here
against SG-165's 2.70 s at hp 180), and life is real, but it bought him no swings
at a captain who moves. The LETHAL numbers survive in shape and shift in size:
2.2 swings and 77 damage here against SG-165's 2.49 and 84.6.

---

## The rig fault this pair found, and it is SG-190 one frame deeper

SG-190 established that a hand-stepping rig must be inside a physics frame or
`move_and_slide()` integrates the idle wall clock. What it did not establish is
that being inside a physics frame is enough.

Measured in `tools/crew_probe.gd`, after `Engine.physics_ticks_per_second = 20`
and two `await physics_frame`s:

| | |
|---|---|
| `Engine.physics_ticks_per_second` | 20 |
| `Engine.is_in_physics_frame()` | true |
| `root.get_physics_process_delta_time()` | **0.01667** |
| what `move_and_slide` actually integrated | **0.05** |

The walk identity caught it: `ground / (|v| × DT)` came back **2.87**, and
0.05 / 0.01667 is 3.00. A rig that asks for a clock and is told it has one is not
a rig that has one.

`crew_probe` sets no tick rate at all — it takes the rate the engine is already
running at, steps at exactly that, and proves the identity above every result.
Three times as many steps per run, and worth it.

The same identity was added to `melee_probe`, where it reads **1.00** in every
arm the subject actually walks in — so that rig's 0.05 step is sound and its
numbers stand. It reads 0.33 when the subject is pressed against the captain's
collider, which is `move_and_slide` sliding along a body and not a clock fault at
all; the sample is restricted to frames with room to walk for exactly that
reason. That near-miss is worth remembering: **0.33 is what a real clock fault
looks like, and it is also what standing next to something looks like.**

---

## SG-195 / OPS-BAL-00 — distinct-seed balance evidence

No gameplay, bot policy or balance value changed. `tools/balance.gd` now
generates `BAL1` through the requested count, preserves the positional CLI, and
audits repeated complete-row fingerprints before statistics. Repetitions never
increase effective sample size; every interval reads one accepted observation
per distinct seed.

The audit found one more instrument contaminant before it proved the repair.
On a fresh process, `BAL1` rep 1 differed while reps 2 and 3 matched exactly;
`BAL2` also repeated exactly. An awaited physics sync at the start of `_one`
did not remove that first-run difference. One explicitly printed
`BAL-WARMUP` run, excluded from `raw_results`, every interval and effective
`n`, establishes the process-wide physics/resource state before sampling.

Verification:

```text
godot --path . --headless --script tests/parity_test.gd
1133/1133 checks passed

balance instrument · one hundred twenty requested samples are BAL1 through BAL120
balance instrument · repeated fingerprints never inflate effective n
balance instrument · variation within one seed fails the instrument
balance instrument · positional heat article and reps arguments remain compatible
balance instrument · every reported interval reads the distinct-seed count

godot --path . --headless --script tools/balance.gd -- 2 0 none 2
BAL1 #1 == BAL1 #2
BAL2 #1 == BAL2 #2
SAMPLE requested seeds 2 · distinct seeds 2 · repetitions 2 · effective n 2
DETERMINISM AUDIT PASS · every seed returned one fingerprint
```

Rollback is the SG-195 commit. Historical balance reports that treated repeated
copies of the old six seeds as independent samples remain historical artifacts;
they cannot satisfy an expansion G3 gate.

Post-merge prerequisite invocation, against `2c1ee5e6ed819f5351339daba840b881961db511`:

```text
godot --path . --headless --script tools/balance.gd -- 120 0 none 1
SAMPLE requested seeds 120 · distinct seeds 120 · repetitions 1 · effective n 120
held 74/120 = 62% · 95% interval 53%..70% · worst-case resolution ±9 points
damage taken mean 203.675 · sd 88.477 · 16% difference resolvable at this n
SAMPLES n=120 requested=120 distinct=120 reps=1 held=74 wave_mean=11.500
```

Raw log: `.codex-work/gameplay-expansion/OPS-BAL-00/SG-195/balance-120-distinct.log`
SHA-256: `17B90ACDB4EE9B3970402FC4AF582C24E8228BA6E9E6DBFF823634FD9443C295`

---

## SG-196 / BASE-00 — immutable expansion baseline

Baseline code commit: `2c1ee5e6ed819f5351339daba840b881961db511`.
This packet changed no project file. Its raw evidence and the exact ignored
fixture source live at `.codex-work/gameplay-expansion/BASE-00/SG-196/`.

G0 was rerun after the coordinator claim: **1133/1133**, exit 0, zero script
errors. `full-harness.log` SHA-256 is
`CD7903ADD81193273BD259B8F7892363C95EA58822EF465FD690E7F674C0FA9A`.

### Spawn queues and RNG boundaries

Each cell is the first 12 hex characters of the SHA-256 of the queue's
`var_to_bytes` payload, waves 1 through 12 from left to right. Full hashes,
byte lengths, raw queues and before/after state strings are in
`queue-fixtures.json` (SHA-256
`1FD227E3C4B954C80755EF4530673BA9AF8CB781A98D166FD1971AD432E8D69A`).

```text
STOW      c696b2cd6ee0 241f15ff2d35 026680b84ce0 2663fcaf209b 9eea55732563 5f0ea88de367 b7c5ee72684f 53f96e69c0e2 6085e3915693 fa2025f009fc 517b10e2f259 7244e946bdd3
TEMPO     c696b2cd6ee0 241f15ff2d35 f6ca6b3fd131 2663fcaf209b 133918efc59f 3d8849d0e3dd b57707a444c4 53f96e69c0e2 682fafe1c8e0 fa2025f009fc 01689d7db4a1 7244e946bdd3
WATCH1    c696b2cd6ee0 241f15ff2d35 f6ca6b3fd131 2663fcaf209b bb1f813c76aa d17c97640fae a5f6f4968f04 53f96e69c0e2 54cfe8af91eb 8f8b480b426f 517b10e2f259 7244e946bdd3
WATCH2    c696b2cd6ee0 241f15ff2d35 f6ca6b3fd131 2663fcaf209b aaa10d78fda4 3d8849d0e3dd eb57ad289657 53f96e69c0e2 a607e6820bd3 ac05dcd7af44 07b96cbb0b5a 7244e946bdd3
WATCH3    c696b2cd6ee0 241f15ff2d35 6c76e46f9dce 2663fcaf209b e2da5e304177 3d8849d0e3dd 0827f543eb0d 53f96e69c0e2 6085e3915693 fa2025f009fc fb44300ed701 7244e946bdd3
COLOSSUS  c696b2cd6ee0 241f15ff2d35 f6ca6b3fd131 2663fcaf209b 47bb23eb7aa6 6871652e54c1 b57707a444c4 53f96e69c0e2 6085e3915693 0bf34629d5db 517b10e2f259 7244e946bdd3
```

All **72/72** calls left both `rng.state` and `visual_rng.state` byte-for-byte
unchanged.

### Per-wave balance baseline

The ignored fixture uses the production balance bot, 0.05 s hand-step,
20 Hz physics boundary, one excluded warm-up, and `BAL1…BAL120` once each. Its
two-seed smoke reproduced the production tool exactly before the full capture:
BAL1 lost on wave 11 with 66 taken; BAL2 held wave 12 with 244 taken. The full
totals also match: 74/120 held, wave mean 11.500, damage taken mean 203.675.

Rows below are conditional on reaching the wave; duration includes a losing
wave's time to defeat. The raw 120 run rows and Wilson intervals are in
`metrics.json` (SHA-256
`58F92F50D9885262F8E720973225FBFB31D740E7369C32ACE61C6C686FA88FAB`).

| Wave | Reached | Held | Duration mean ± sd | Captain damage mean ± sd |
|---:|---:|---:|---:|---:|
| 1 | 120 | 120 | 13.91 ± 1.55 s | 0.00 ± 0.00 |
| 2 | 120 | 120 | 15.17 ± 2.04 s | 0.75 ± 2.64 |
| 3 | 120 | 120 | 18.39 ± 2.26 s | 2.66 ± 7.61 |
| 4 | 120 | 120 | 29.61 ± 1.99 s | 3.02 ± 7.08 |
| 5 | 120 | 120 | 19.95 ± 2.52 s | 1.55 ± 5.90 |
| 6 | 120 | 119 | 28.26 ± 2.76 s | 15.69 ± 20.49 |
| 7 | 119 | 116 | 25.08 ± 3.97 s | 6.13 ± 10.33 |
| 8 | 116 | 114 | 31.68 ± 4.53 s | 12.89 ± 18.99 |
| 9 | 114 | 114 | 25.42 ± 3.03 s | 6.13 ± 10.69 |
| 10 | 114 | 112 | 29.42 ± 4.60 s | 19.14 ± 19.95 |
| 11 | 112 | 85 | 36.06 ± 6.67 s | 18.88 ± 22.60 |
| 12 | 85 | 74 | 26.61 ± 5.46 s | 169.20 ± 45.20 |

The general quotable resolution remains the production tool's printed result:
held **74/120 = 62%**, Wilson 95% **53%..70%**, worst-case **±9 points**;
damage taken CV 43%, so this `n` can establish a mean difference of **16% or
larger**, not less.

### Damage attribution v1

- `damage-attribution-v1-ember.txt`: BAL1, Ember Gale and Ember Whip slotted,
  every raw slot's damage/hits/kills/casts plus basic/allies/deck rows. SHA-256
  `DCC24A7BEE9D386A30DD65113A392F1AFF582413C7CC88C600E4D85700EC8699`.
- `damage-attribution-v1-non-ember.txt`: BAL2, all four slotted skills Frost;
  the authored Ember Cleave auto remains and is labelled rather than hidden.
  Every raw slot row is retained. SHA-256
  `ABB6CB0B44D5A69E15D029A3B19E902F2B82B45A59004827F96C84127D90829F`.

These filenames deliberately carry `damage-attribution-v1`; no player-facing
copy changed.

### Visual fixtures

- `visual/ordinary-arrival.png` — wave-1 tender on its hold from the existing
  arrival tool, SHA-256
  `EB895DDC66C88FDF12B4BB7771A33CE03F8B25CA873576BC72C1F4E0203694C6`.
- `visual/blackout.png` — WATCH2, wave 8, live blackout event at the normal deck
  camera, SHA-256
  `3852EB42CEBCFC66B635DBB6175120BB5A262D700478068F073F05C691FB72AB`.
- `visual/blackout-control.png` — same seed, wave, tree and camera with only the
  event flag off, SHA-256
  `54801150D979DBA932AE8F4A6E85A533A4A362AFAD478A11F4566C7B39CD8006`.
  A four-pixel-stride read found **57.463%** of samples changed above a two-level
  RGB floor, mean absolute change 8.786/255.
- `visual/boss.png` — the existing Colossus tool's live-AI walk to a 419-unit
  camera-safe stand-off, `lit 0.1549`, `blown 0.043%`, SHA-256
  `105C51FD4497BAFDEC8DCF1483BDD8430F07F7330B343231CB52BCFDE7227310`.

The coordinator visually opened all four PNGs: arrival, blackout lighting and
Colossus are present and readable; this is evidence capture, not a G5 verdict.

### Save/RNG/rollback findings

The fixture replaces the loaded workshop with `SkyGearWorkshop.fresh(true)`,
sets `game.log_runs = false`, and writes only under the evidence directory.
No player save or run-log entry is written. Queue construction consumes neither
RNG stream. BASE-00 adds no field, owner, reset path or reader. Rollback is
evidence-only: remove `.codex-work/gameplay-expansion/BASE-00/SG-196/` and
revert the coordinator board/night-log record. No gameplay seam exists to turn
off because no gameplay changed.

Commands, all from `skygear-godot/`:

```text
godot --path . --headless --script tests/parity_test.gd
godot --path . --headless --script ../.codex-work/gameplay-expansion/BASE-00/SG-196/base_fixture.gd -- 2
godot --path . --headless --script ../.codex-work/gameplay-expansion/BASE-00/SG-196/base_fixture.gd -- 120
godot --path . --script tools/arrival_shot.gd
godot --path . --script builds/base-evidence/blackout_shot.gd
godot --path . --resolution 1600x900 --script tools/boss_shot.gd -- ../.codex-work/gameplay-expansion/BASE-00/SG-196/visual
```

Every command completed. The first attempt to execute the blackout script from
outside `res://` emitted engine-exit resource cleanup noise after writing a
valid image; the accepted command above runs the same fixture from the ignored
`builds/base-evidence/` area and exits cleanly. No failed fixture was redefined
as baseline evidence.

---

## SG-197 — EV-01 G3 contract is missing, packet BLOCKED before code

EV-01's mechanical prerequisite is green: BASE-00 is accepted at `0a6d34a`,
and no active claim occupies `scripts/game.gd` or `tests/parity_test.gd`.
The packet is still not `READY` because its measurement verdict cannot be
preregistered from the authoritative text without inventing two numbers.

Design §16.2 requires forced grammar arms to move **a pre-declared per-wave
behavioral distribution** beyond the instrument's printed resolution while
not moving held-rate by more than **the accepted difficulty band**. It names
neither the primary behavior nor a numeric band. The older focused design names
damage taken and wave duration, but neither directly proves that PINCER versus
ASSAULT changed the captain's positional decision. The implementation plan
§7.1 requires the primary statistic, arms, seam, distinct seeds, resolution and
pass rule before a G3 packet becomes READY; readiness step 6 says to file a
decision and keep it BLOCKED if those details would otherwise be invented.

Recommended contract, chosen before any EV-01 implementation or result exists:

- primary: absolute lateral ground travelled by the Captain during wave 6;
- arms: forced PINCER versus forced ASSAULT, identical Heat 0, class, draft,
  fittings, bot and `BAL1…BAL120`; one observation per distinct seed;
- behavioral pass: mean changes by at least 10% **and** the 95% comparison
  clears the targeted tool's printed resolvable effect;
- feature-off/difficulty arm: live Muster versus `SKYGEAR_MUSTER_FLAT`, the
  same 120 distinct seeds;
- difficulty guard: absolute held-rate delta no more than 9 percentage points,
  the corrected BASE-00 command's printed general resolution;
- repetitions: one; permitted tuning: none; any same-seed audit variation is
  instrument failure, not more `n`;
- verdict: positional miss or difficulty-band breach cuts Muster through its
  declared flat seam and retains the report.

Alternative if Alex prefers the older design's emphasis: make per-wave damage
taken the primary and clear time supporting. That is easier to collect but can
pass when the same positional decision merely became more punishing, so the
coordinator recommends lateral travel.

No gameplay, harness or packet file was edited. The decision is item 1 in
`/NEEDS_ALEX.md`.

### Owner verdict and dispatch

Alex approved the recommendation verbatim before implementation: **use the
recommended Muster gate**. SG-197 is resolved, the item is removed from
`/NEEDS_ALEX.md`, and EV-01 is freshly claimed as SG-198 against baseline and
serialization cursor `6e5c9a1`.

The frozen contract is the recommendation above without reinterpretation:
wave-6 absolute lateral Captain travel, forced PINCER versus forced ASSAULT,
120 distinct seeds and one observation each; the mean must move by at least
10% and the targeted tool's 95% comparison must clear its printed resolution.
Live Muster versus `SKYGEAR_MUSTER_FLAT` must remain within an absolute
9-point held-rate delta on the same 120 seeds. Repetitions are one, permitted
tuning is none, and any same-seed variation is an instrument failure.

SG-198 receives one packet, the ten verbatim `muster ·` checks from design
§16.2, the allowed-file list recorded on the board, and stop conditions for
contract conflict, RNG/save mutation, an undeclared file, or an unrelated red
check. The collision-heavy queue is otherwise clear.

---

## SG-198 / SG-199 — Muster stops on an ordering-contract conflict

SG-198 was built red-first. The ten exact `muster ·` names first produced
**1133/1143**, with only those ten new checks red. The pure planner, twelve-plan
cache, deep-copy reader, local RNG, four grammars, budget filter and feature-off
seam now parse. The ten substantive checks themselves are green: all four
forced grammars satisfy their postconditions, 288 plans keep valid types/lanes
and the final queue schema, 24 fixed seeds produce 12 distinct wave-6 plans,
flat collapses the same signature to one, cached/twin/on-demand plans match,
and planning leaves both live RNG states unchanged.

The full harness is nevertheless **1142/1143**, and the remaining red is not a
gameplay defect that can be tuned away:

`tempo · STEADY deals today's queue byte-identically`

Design §16.2 requires two incompatible things. The flat seam must reproduce the
authored queue byte-for-byte, and final plans must sort by time, source, lane
and member. HEAD's queue builder sorted by time alone. Equal-time entries
therefore have a deterministic but non-lane-ordered tie pattern. Wave 3 proves
the conflict:

- legacy time-only sort, lane order at 0.00 / 0.22 / 0.44 / 0.66 seconds:
  `012 / 021 / 210 / 102`;
- required stable sort at those same times: `012 / 012 / 012 / 012`.

The entries, types and times are identical; only equal-time array order differs.
No assignment of the real lane field can make a time/source/lane/member sort
produce the old permutation. Weakening the legacy control would make the
required byte-identity check incapable of firing, while silently using the old
sort everywhere would violate the fixed build order.

SG-199 therefore stops SG-198 before G3 or merge. Coordinator recommendation:
make the rollback seam explicit — `SKYGEAR_MUSTER_FLAT` alone uses the legacy
time-only sort, while live Muster uses the new stable order. The alternative is
to adopt stable ordering under flat and explicitly waive byte identity. The
decision is item 1 in `/NEEDS_ALEX.md`. Implementation remains unmerged.

### Owner verdict

Alex approved the recommended seam. Flat Muster preserves HEAD's legacy
time-only equal-time ordering; live Muster uses the required stable
time/source/lane/member ordering. SG-199 is resolved before G3 or merge, its
item is removed from `/NEEDS_ALEX.md`, and SG-198 resumes verification.

### G3 verdict — CUT

The implementation closed G0/G1/G2 at **1143/1143**, including all ten exact
`muster ·` checks. The preregistered forced-grammar experiment then ran Heat
0, the same bot/class/draft/fittings, `BAL1…BAL120`, one observation per
distinct seed and excluded warm-ups:

- ASSAULT mean absolute lateral ground in wave 6: **6801.662**;
- PINCER mean: **7343.325**;
- paired difference: **541.663**, or **7.96%** of ASSAULT;
- paired 95% half-width: **101.445**;
- printed targeted resolution: **1.49%**;
- reached wave 6: **120/120 in both arms**.

The movement is real and measured far beyond the instrument's resolution, but
the approved gate required at least **10%**. Verdict: **FAIL**. Permitted tuning
was none, so no roster, cost, lane recipe or threshold moved and the held-rate
arm was not run after the primary kill condition failed.

The complete candidate is preserved at `94fbdc2` and cut whole by
`8ce043e`. Post-cut harness is **1133/1133**, zero script errors. Raw log:
`.codex-work/gameplay-expansion/EV-01/SG-198/g3-forced-120.log`, SHA-256
`FF1EF0BF4E2A7BF50998DD190FB5C294F059C3298692EAAB47EE07CDBB0A987D`.

Itch build **63-muster-candidate** briefly exposed the green implementation for
owner testing while measurement ran. Once G3 failed it was superseded, without
calling the experiment shipped, by verified rollback build
**64-muster-cut**. EV-02, EV-03, EV-05 and LV-01 stay blocked.

---

## SG-200 — deck-edge owner verdict published

Alex's verdict was exact: **“Deck edge: go with breast rail and capping
deleted.”** The shipping defaults are now strake mode 2 and end mode 1: no
brass strake/apron capping, no low-rail substitute, and fourteen of the
owner-authored rail modules across each breast rail. The SG-180 comparison
states remain available only as rollback evidence.

The full harness passed **1134/1134**. The exact live-state checks are
`deck · the owner-approved breast rails ship with the strake capping deleted,
and rebuilding keeps that verdict` and `deck · the shipped breast rails cannot
stop her`. A GPU sweep of `capping shipped` wrote fresh stem, port, and mid
frames under `.shots/sg157/sg180-shipped/`.

Commit `012d98c` was exported to `builds/itch/SkyGear-Windows.zip`
(SHA-256
`BC2460BF9204082FD37B8DF65512A7B4CCCC704D93902CE12F7F541F03B81F47`)
and published as itch Windows **65-deck-edge-verdict**, build **#1858433**.
Rollback is commit `012d98c` plus itch build **64-muster-cut**
(#1858373). The resolved deck question is removed from `/NEEDS_ALEX.md`.

---

## SG-201 — EV-01 lane-contrast repair cut

The separately claimed repair restored SG-198 and changed one selection rule:
when PINCER has a legal candidate that actually moves an authored source to a
different lane, type-only substitutions no longer dilute that forced arm. It
adds no RNG draw and changes no cost, roster, time, threshold, queue schema,
event exemption, Heat setting or bot policy.

The focused check
`muster repair · PINCER prefers a lane reassignment when one is legal` passed
24/24 fixed wave-6 seeds at unchanged threat 24.75, roster and timing. The full
candidate harness passed **1145/1145**, including the original ten Muster
checks, local RNG checks and the five editor/save guards.

The frozen G3 then ran forced ASSAULT versus forced PINCER at Heat 0 on
`BAL1…BAL120`, once per distinct seed:

- ASSAULT mean absolute lateral Captain ground: **6801.662**;
- PINCER mean: **7387.775**;
- paired difference: **586.113**, or **8.62%**;
- paired 95% half-width: **102.610**;
- printed resolution: **1.51%**;
- reached wave 6: **120/120 in both arms**.

That movement is real and well resolved, but it still misses the predeclared
**10%** minimum. Per SG-201's stop condition there is no second lever and the
live-versus-flat difficulty arm was not run. Candidate `b2cae3e` is preserved
and cut whole by `2ce2313`. The post-cut harness is **1134/1134**, zero script
errors. Raw evidence is under
`.codex-work/gameplay-expansion/EV-01/SG-201/`; the G3 log SHA-256 is
`FBC6F9A32AD324E906E3CE234AEF21DFAB229080A6EB05FD4F77F5E93C46D13F`.

No failed SG-201 candidate was pushed. Itch build **65-deck-edge-verdict**
remains the safe live build. EV-02, EV-03, EV-05 and LV-01 remain blocked; no
owner decision is required, so `/NEEDS_ALEX.md` is unchanged.

---

## SG-202 — AB-01 Beam candidate measured and published for G5

Beam is now a simulation-owned 0.36-second channel: four ticks at 0.00, 0.12,
0.24 and 0.36 seconds, seven base damage each. Cooldown and Overpressure are
paid once on press; ordinary walking is scaled to 0.60; dash or Bleed Jet
cancels the remainder; hurt does not. Live casts reread aim each tick while an
explicit `Vector2` stays fixed. Completion resolves hulk splash and Residue
once at the current ray midpoint.

Every production enemy now receives a unique nonzero spawn serial. Beam threads
one optional element-once dictionary through the line, primary damage,
`take_damage`, element mutation and Beam crit explosion. An accepted body
claims its serial before mutation; immunity claims nothing. The synchronous
kill-explosion path still receives null, so it remains independent.

Red-first harness was **1134/1147**: exactly the thirteen new checks failed.
The exact candidate is **1147/1147**, zero script errors. The named checks are:

- `beam · four scheduled ticks total twenty-eight base damage`;
- `beam · cooldown and Overpressure are paid once on press`;
- `beam · maximum ordinary cooldown reduction cannot produce a cooldown below
  channel time; a free cast still cannot re-enter its live channel`;
- `beam · an explicit target is stable and live aim can turn between ticks`;
- `beam · walking is sixty percent while dash and jet cancel`;
- `beam · another cast and the basic wait for completion`;
- `beam · pause, hit-stop, reset and death own no orphan tick`;
- `beam · one cast and four hits share one slot attribution`;
- `beam · one body receives one element application per channel while damage
  and crit remain per tick; a swept-in body receives one and the next cast
  resets`;
- `beam · its crit explosion shares the element-once context while a kill
  explosion caused inside the same call tree receives null and applies
  normally`;
- `beam · last_land is the current ray midpoint, not the line endpoint`;
- `beam · hulk and Residue resolve once, never once per visual frame`;
- `beam · the forced clip shows a readable start, held line and release with
  zero probe noise`.

G3 replayed the same scripted moving-target trajectories on
`BEAM1…BEAM120`. Two repetitions audited determinism only; effective n stayed
120 per arm. Live-tracked mean primary damage was **28.0**, explicit-locked mean
**11.6083**, paired difference **16.3917** with paired 95% half-width
**0.5965**. The full-stay control remained **28.0** and every accepted body saw
one selected-element application per channel. The geometry fixture disables
crit and reaction damage, so both breakout columns are explicitly **0.0**;
the enabled recursive edges are covered by the focused check above rather than
being folded into the primary movement statistic.

The exact-candidate GPU probe wrote start, held and release plates under
`.codex-work/gameplay-expansion/AB-01/SG-202/visual-exact-candidate/`.
Same-state noise is **0.00%** and the channel is absent after release.

Candidate commit `508523c` produced
`builds/itch/SkyGear-Windows.zip`, SHA-256
`39B3B3AB64ABDFEEE906B95A2A0E61A9B1E4A06494FF5953CA09A98E9ACAA587`.
It is live as itch **66-beam-channel-candidate**, build **#1858483**. Rollback is
itch build **65-deck-edge-verdict** (#1858433) and code cursor `af7d8ce`.
This is a candidate, not a combat release: Alex's named G5 is pending in
`/NEEDS_ALEX.md`, and IN-00 still holds the release-critical combat train.

---

## SG-203 — EL-00 preclaim attribution-control defect filed

The EL-00 readiness audit found that BASE-00's artifact named
`damage-attribution-v1-non-ember.txt` means “no Ember **slotted skill**,” not
“no Ember damage source.” Its own build line contains `Ember Cleave (auto)` and
its basic bucket records 5,882 damage. Because EL-00 moves that auto-applied
burn through the ordinary attributable damage funnel, the report is expected
to change and cannot be §17.4's required stable non-Ember control.

The original accepted artifact remains immutable. SG-203 is a coordinator-owned
evidence correction, not a gameplay packet and not an owner decision. After
AB-01 passes G5, but before EL-00 is claimed, a copied driver will set the
Captain's existing `auto_element` seam to Frost before `begin_run`, retain BAL2,
and assert that the auto and every equipped skill are non-Ember, no target burns,
and no BURN reaction row exists. It will freeze Beam-stable attribution-v1 Ember
and true-non-Ember controls plus the 120-distinct-seed feature-off arm. No
project file or tuning value may change.

---

## SG-202 — human G5 passed

Alex's exact verdict on itch build **66-beam-channel-candidate** was:
**“Beam feels sweet actually.”** This satisfies the named G5 fixture for aim
commitment, the 60% ordinary-movement cost, and the dash/Bleed Jet escape.
AB-01 advances through `HUMAN-VERIFIED` to `MERGED`; its implementation remains
`508523c`. The Beam decision was removed from `/NEEDS_ALEX.md`.

This is acceptance of the packet, not publication of the combat train. IN-00
still holds the release-critical combat set. SG-203 is now the next coordinator
action, followed by EL-00.

---

## SG-203 cut; SG-204 true-non-Ember control repair filed

SG-203's 120-seed production arm completed and its repeat audit passed, but its
predeclared true-non-Ember gate failed honestly. Forced-Frost BAL2 had no Ember
auto or equipped skill and no BURN reaction row, yet five targets acquired burn.
The follow-up probe found one live production `fire_field` beside each first
acquisition on waves 7, 8 and 11. The fields were at the authored lantern
positions `(-610, -100)` and `(620, -420)`; `kill_explode` was zero.

The conflict is structural: `_update_fire_fields()` applies hardcoded Ember through
the ordinary damage circle independently of the Captain's build. Therefore a
full production run is not a true no-Ember control merely because every
player-owned element is Frost or Steam. The failed report remains labelled
FAIL and SG-203 is cut; no project file or tuning value changed.

SG-204 is a separately scoped evidence repair. It retains SG-203's valid
120-distinct-seed production feature-off arm and Ember report, then adds a
focused production-funnel control with no props or fire fields by construction.
That control must be byte-repeatable, use only non-Ember player sources, acquire
no burn, print no BURN row, and prove the negative side by adding one real fire
field and watching the no-burn assertion fail. EL-00 remains blocked until this
repair passes.
